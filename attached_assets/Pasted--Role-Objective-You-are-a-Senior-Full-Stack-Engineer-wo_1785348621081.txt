# Role & Objective
You are a Senior Full-Stack Engineer working on the ZMAN Next.js PWA. We need to pause all UI/UX modifications. The owner has reported that the numbers displayed on the live Dashboard are mathematically incorrect and do not match the real financial state in the database.

Your mission right now is to perform a strict **Data & Logic Audit** to find out why the frontend numbers are wrong. 

# CRITICAL INSTRUCTION: STOP AND ANALYZE FIRST
DO NOT WRITE ANY CODE OR MAKE ANY UI CHANGES YET. 

I need you to trace the data flow for the Dashboard components, from the database queries/hooks to the frontend display, and identify the mathematical or logical flaws.

Please investigate the following specific numbers and report back:

1. **Operational Profit & COGS (FinanceComparePanel):**
   - How is "COGS" (Cost of Goods Sold) calculated in the backend? Is it actually calculating the cost of *delivered* orders only, or is it pulling total purchases?
   - How is the "Operational Profit" calculated? Does it correctly subtract COGS and true operating expenses, or is it mistakenly subtracting inventory purchases?

2. **Cash Flow & Liquidity (LiquidityFlowPanel):**
   - Does the "Cash Out" accurately reflect all money that left the cash/bank accounts? 
   - Are owner draws and inventory purchases being mixed up in a way that skews the total cash balance?

3. **Health Card Metrics (HealthCard):**
   - "Cash Available": Does this match the actual sum of Cashbox + Bank accounts in the DB?
   - "Net Profit after withdrawals": How is this calculated (`netProfit - ownerDraw`)? Is `ownerDraw` pulling the correct records?
   - "Net Project Value" (`equity.totalCents`): Does this equation (`opening + injections − draws + retainedProfit − capitalAdditions`) balance correctly with the actual assets and liabilities in the DB?

# Required Output
Provide a detailed audit report containing:
1. **The Flaw:** For each metric, explain exactly where the calculation goes wrong (e.g., "COGS is pulling all purchases instead of just delivered items").
2. **The DB/Backend Evidence:** Mention the exact file, hook, or SQL query where this faulty logic resides.
3. **The Proposed Fix:** How should the query/calculation be corrected to reflect the true financial reality?

Wait for my approval of your fixes before you implement any code.