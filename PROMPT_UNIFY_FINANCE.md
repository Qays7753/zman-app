# مهمة: توحيد واجهة الإدخال المالي في Zman + إصلاح فقدان بيانات المورّد

> اقرأ هذه الوثيقة كاملة قبل كتابة أي كود. الترتيب مقصود: المرحلة صفر خلل نشط يفقد بيانات، والمراحل التالية تعتمد عليها.

---

## 0. السياق — اقرأه أولاً

المشروع monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في جذر المستودع قبل أي شيء** — خصوصاً §2 (النموذج المالي)، §3 (حمايات مثبَّتة)، §10 (RTL والنقود)، §11 (بوابات الفحص).

المالك يستخدم النظام يومياً وعلى الهاتف 95% من الوقت. الشكوى التي أطلقت هذه المهمة بكلماته:

> «ضايع في المالي، شو الفرق بين المصاريف والمشتريات؟ عامل مشكلة وتكرار بالكونسبت. بدي واجهة واحدة للأمور المشتركة، الأفضل والأبسط في الاستخدام، والي يخلي المستخدم فاهم شو يعمل بكل خطوة.»

### التشخيص المؤكَّد (مفحوص على الكود، لا تخمين)

يوجد **ثلاثة نماذج** تفعل أشياء متداخلة:

| الملف | الأسطر | الحالة |
|---|---|---|
| `src/features/finance/components/SmartFinanceForm.tsx` | ~950 | الموحَّد — ثلاثة أوضاع (`expense` / `purchase` / `asset`) |
| `src/features/finance/components/ExpenseForm.tsx` | 334 | قديم — **بلا أي مستدعٍ** (ميت) |
| `src/features/finance/components/PurchaseForm.tsx` | 605 | قديم — مستدعىً في موضع واحد فقط |

خريطة الاستدعاء الفعلية:

| المدخل | النموذج المستعمل |
|---|---|
| إنشاء من تبويب المصاريف (`ExpensesTab.tsx:424`) | ✅ `SmartFinanceForm` |
| تعديل من تبويب المصاريف (`ExpensesTab.tsx:463`) | ✅ `SmartFinanceForm` |
| تعديل من تبويب المشتريات (`PurchasesTab.tsx:409`) | ✅ `SmartFinanceForm` |
| **إنشاء من تبويب المشتريات (`PurchasesTab.tsx:392`)** | ❌ `PurchaseForm` القديم |

**هذا هو مصدر شعور المالك بالتكرار:** يُنشئ بنموذج ويُعدّل بآخر، لنفس السجل، بقدرات مختلفة.

---

## المرحلة صفر (عاجلة ومستقلّة) — إصلاح محو المورّد

### الخلل

`SmartFinanceForm` في وضع التعديل يُرسل `supplier: ""` دائماً:

- `SmartFinanceForm.tsx:505` (مسار التعديل) — `supplier: "",`
- `SmartFinanceForm.tsx:531` (مسار الإنشاء) — `supplier: "",`

والسبب الجذري أعمق من السطر: **الواجهة `SmartFinanceFormInitialData` (حوالي السطر 100-140) لا تحتوي حقل `supplier` إطلاقاً** — فالمكوّن لا يملك القيمة أصلاً ليحافظ عليها.

**الأثر النشط اليوم:** تبويب المشتريات يستعمل `SmartFinanceForm` للتعديل بالفعل. أي شراء قديم يحمل اسم مورّد → يفتحه المالك ليعدّل التاريخ أو المبلغ → **يُمحى اسم المورّد بصمت**.

### المطلوب

1. أضف `supplier?: string;` إلى `SmartFinanceFormInitialData` مع تعليق عربي يشرح لماذا.
2. في مسار التعديل (~505): `supplier: initialData.supplier ?? ""`.
3. في `PurchasesTab.tsx` عند بناء `initialData` لـ `SmartFinanceForm` (~409 وما بعده): مرِّر `supplier: activePurchase.supplier`.
4. تحقّق أن `activePurchase` يحمل `supplier` فعلاً — إن لم يكن، تتبّع مصدر البيانات في `src/features/finance/queries.ts` وأصلح الاختيار.

⚠️ **لا تُنشئ حقل إدخال مورّد في هذه المرحلة** — هذه المرحلة حفظ للقيمة القائمة فقط. الحقل المرئي في المرحلة 1.

---

## المرحلة 1 — إغلاق فجوة الحقول قبل التوحيد

قارنت النموذجين حقلاً بحقل. الفجوة الوحيدة الحاجبة هي **المورّد**:

| الحقل | `PurchaseForm` القديم | `SmartFinanceForm` | القرار |
|---|---|---|---|
| المورّد | ✅ حقل نصّي (~327) | ❌ غائب | **أضفه** — اختياري |
| سعر الوحدة | ✅ إدخال مباشر (~162-189) | يُشتقّ: `totalCents×1000÷qty` | اتركه مشتقّاً — أبسط، والدقّة محفوظة بـ micro-cents |
| رأسمالي / طبيعة التكلفة | ✅ ظاهر (~365، ~394) | مخفي — يُثبَّت `variable` | اتركه — وضع «أصل للورشة» يغطّي الحالة |
| منتقي الكتالوج | `tracked` فقط (~64-67) | `tracked` فقط (~190) | متطابق — لا عمل |

### المطلوب

أضف حقل **المورّد** (اختياري) إلى وضع «شراء مواد» في `SmartFinanceForm`:
- في `purchaseSchema`: `supplier: z.string().max(200).default("")`.
- حقل إدخال في واجهة وضع `purchase`، بعد اسم الصنف.
- مرِّره في مساري الإنشاء والتعديل بدل `""` المثبَّت.
- `max(200)` إلزامي — قيد `purchase_supplier_length` في قاعدة البيانات يرفض الأطول.

---

## المرحلة 2 — التوحيد

1. في `PurchasesTab.tsx:392` استبدل `PurchaseForm` بـ `SmartFinanceForm`.
   - يحتاج `onSuccess` و`onClose` (لا `onSubmit`/`isSubmitting`) — `SmartFinanceForm` يستدعي الـ mutations داخلياً.
   - احتذِ بنمط `ExpensesTab.tsx:424` حرفياً.
   - افتح النموذج على وضع `purchase` افتراضياً في هذا المدخل (المستخدم ضغط «تسجيل مشتريات»).
   - تحقّق أن `handleCreate` و`createMutation` لم يعودا مستعملين؛ احذفهما إن صارا ميتين.
2. احذف `ExpenseForm.tsx` و`PurchaseForm.tsx` بعد التأكّد بـ grep أنهما بلا أي مستدعٍ.
   - `DepreciationPromptModal.tsx:21` يذكرهما في **تعليق** فقط — حدّث التعليق، لا تحذف الملف.

---

## المرحلة 3 — الوضوح (هذا هو جوهر طلب المالك)

المالك قال صراحةً إن مشكلته ليست نقص التوضيحات، **لكنه طلب واجهة «تخلي المستخدم فاهم شو يعمل بكل خطوة»**. الوضوح هنا ليس زينة — هو ما يجعل الواجهة الواحدة تُغني عن السؤال «مصاريف أم مشتريات؟».

الأوضاع الثلاثة معروضة حالياً في `SmartFinanceForm.tsx:722-738` كأيقونة + كلمتين، **بلا أي شرح لأثر الاختيار**.

### المطلوب

1. **سطر توضيحي تحت شريط الأوضاع** يتغيّر مع الوضع المختار، يقول أثره على الربح بلغة المالك:
   - `expense` → «يُخصم من ربح هذا الشهر كاملاً.»
   - `purchase` → «مادة تدخل منتجاتك. إن ربطتها بصنف متتبَّع، تُخصم تكلفتها عند البيع لا عند الشراء.»
   - `asset` → «لا يُخصم من ربح هذا الشهر. يُوزَّع إهلاكاً على عمره النافع.»

   الصياغة الدقيقة لك، بشرط أن تكون **صحيحة محاسبياً ومطابقة لـ§2 من `CLAUDE.md`**. لا تَعِد بسلوك غير مبنيّ.

2. **عند ربط شراء بصنف متتبَّع**، أظهر تأكيداً صريحاً بأن التكلفة لن تُخصم الآن بل عند البيع. تحقّق أولاً مما إذا كان موجوداً (`SmartFinanceForm.tsx:866-923` فيه منطق منتقي الكتالوج) — إن كان، حسّنه؛ لا تُكرّره.

---

## 🔒 خطوط حمراء — كسرها يُفسد أرقام المالك

1. **لا تلمس `src/features/finance/pnl.ts`** ولا أي منطق محاسبي. هذه مهمة واجهة بحتة.
2. **لا migration ولا تعديل مخطط.** `pnpm drizzle-kit generate` يجب أن يبقى يطبع «No schema changes, nothing to migrate 😴». إن اقترح تغييراً، فقد كسرت شيئاً.
3. **`purchase.total_cents` عمود `GENERATED ALWAYS`** — لا يُكتب يدوياً أبداً (§3 في `CLAUDE.md`).
4. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`text-start`/`text-end`. ممنوع `pl-`/`pr-`/`left-`/`right-`/`text-left`/`text-right`.
5. **النقود أعداد صحيحة بالفلس.** `formatFilsToJod` هو المنسّق الوحيد. لا حساب عائم.
6. **لا أرقام مالية مُفبركة ولا بيانات وهمية عند الفراغ أو الخطأ** — أي fallback يعرض رقماً مختلقاً مرفوض قطعياً (§1 في `CLAUDE.md`).
7. **أهداف اللمس ≥ 44 بكسل**، ويُفحص على عرض 360px و390px: لا تمرير أفقي، لا قصّ، لا تراكب.
8. **لا تحذف زر الحذف** في تبويب المشتريات — `SmartFinanceForm` لا يُدير الحذف، وهو مسؤولية الأب عبر `ConfirmDialog` القائم.

---

## بوابات الفحص — إلزامية قبل الكومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build                # البوابة الحقيقية
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **`typecheck` وحده لا يكفي** — لا يكشف خرق حدود الخادم/العميل. جولة سابقة رُفعت وهي لا تبني أصلاً لهذا السبب. **`pnpm build` هو المعيار.**

⚠️ لا تشغّل `pnpm build` من جذر الـ monorepo — يفشل على حزمة غير ذات صلة.

---

## الاختبار اليدوي المطلوب (نفّذه، ولا تكتفِ بالبناء)

1. **المورّد يصمد:** أنشئ شراءً باسم مورّد → عدّل التاريخ فقط → احفظ → **تأكّد أن اسم المورّد ما زال موجوداً**. (هذا اختبار انحدار المرحلة صفر — الأهم.)
2. أنشئ شراءً من تبويب المشتريات → يجب أن يفتح `SmartFinanceForm` على وضع «شراء مواد».
3. أنشئ مصروفاً من تبويب المصاريف → يعمل كما كان.
4. عدّل شراءً قائماً → الحقول تُملأ صحيحة، والتصنيف الأصلي (`isCapitalAsset` / `costNature`) محفوظ.
5. اربط شراءً بصنف متتبَّع → تأكّد أن حركة المخزون أُنشئت وأن التكلفة **لم** تُخصم من الربح.
6. افتح `/reports` واضغط «فحص الآن» → **الكل PASS أو WARN، ولا واحد FAIL**. إن ظهر `IC-1 = FAIL` أوقف فوراً ولا ترفع.
7. افحص على 360px: كل الأوضاع الثلاثة، بلا تمرير أفقي ولا قصّ.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.** المالك يراجع محلياً أولاً.
- كومت واحد لكل مرحلة (صفر / 1+2 / 3) — تسهيلاً للمراجعة والتراجع.
- رسائل الكومت بالعربية، تشرح **لماذا** لا ماذا فقط.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي — كن صادقاً

اذكر صراحةً:
- ما نُفِّذ وما لم يُنفَّذ ولماذا.
- مخرجات البوابات الثلاث **حرفياً** (لا «نجحت» فقط).
- أي خطّ أحمر اضطررت للاقتراب منه.
- أي اختبار يدوي **لم** تستطع تنفيذه ولماذا.

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود الفعلي: كل استدعاء، كل حقل، ومخرجات البناء. الادّعاء غير الصحيح يُكتشف.
