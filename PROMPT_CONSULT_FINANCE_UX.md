# استشارة — لا تنفيذ: بنية شاشة المالية في Zman

> ## 🛑 هذه مهمة رأي فقط
>
> **لا تكتب كوداً. لا تُعدّل ملفاً. لا تُنشئ كومت. لا تُشغّل أي أمر يُغيّر شيئاً.**
>
> اقرأ الكود بحرّية (Read / grep / بناء الفهم)، وشغّل أوامر قراءة فقط. المطلوب منك **تقرير رأي مكتوب** يُعرَض على المالك ليقرّر. أي تعديل في الملفات يُبطل قيمة الاستشارة.
>
> ملاحظة: أنت من نفّذ جولة سابقة على نفس الملفات (توحيد نماذج الإدخال). هذه المهمة مختلفة — **مراجعة قرار تصميمي، لا تنفيذ**.

---

## 1. لماذا تُستشار

المالك تلقّى مجموعة توصيات من مساعد آخر (Claude) بخصوص إعادة هيكلة شاشة المالية. **هو غير مقتنع بها ويريد رأياً مستقلاً قبل التنفيذ.**

مهمّتك: تقييم التشخيص والحلول المقترحة، **وتقديم رأيك الصريح** — بما في ذلك رفضها أو اقتراح بديل مختلف كلياً. لا تُجامل التوصيات القائمة. إن رأيتها خاطئة أو مبالغاً فيها، قل ذلك بوضوح ولماذا.

**احكم على كل شيء بنفسك من الكود.** ما يلي وصف لِما وجده المساعد الآخر — تحقّق منه، ولا تأخذه مسلَّماً.

---

## 2. السياق — النظام ومستخدمه

- **Zman**: تطبيق إدارة ورشة/محل صغير (نباتات وزراعة). Next.js 15 App Router، `artifacts/zman-app` داخل monorepo بـ pnpm.
- **المستخدم شخص واحد** — مالك الورشة. ليس محاسباً. **يستخدمه على الهاتف 95% من الوقت** (شاشة 360–390px).
- **الهدف الأصلي للمشروع:** «رقم ربح واحد يثق به المالك». بُني بعد شكوى أن ربحه كان يتأرجح.
- **اقرأ `CLAUDE.md` في جذر المستودع** — خصوصاً §2 (النموذج المالي)، §3 (حمايات مثبَّتة)، §5 (مشاكل معروفة)، §10 (RTL والنقود، وأن المستخدم على الهاتف).
- المرجع المحاسبي التفصيلي: `artifacts/zman-app/docs/ACCOUNTING_RULES.md`.

### الشكوى التي أطلقت كل هذا (بكلمات المالك)

> «ضايع في المالي، شو الفرق بين المصاريف والمشتريات؟ عامل مشكلة وتكرار بالكونسبت. بدي واجهة واحدة للأمور المشتركة، الأفضل والأبسط في الاستخدام، والي يخلي المستخدم فاهم شو يعمل بكل خطوة.»

ثم لاحقاً، بعد أن رأى نتيجة الجولة الأولى:

> «أنا كنت مفكر إنهم بدهم يصيروا عبارة عن تبويب واحد.»
> «إذا ما وحّدت، التكرار رايح يضل موجود وكل مرة المستخدم يحتار وين وكيف يدخل.»

وحين عُرضت عليه تسمية «حركاتي» للتبويب الموحَّد **رفضها صراحةً**.

---

## 3. ما نُفِّذ فعلاً (جولة سابقة — مدموجة ومرفوعة)

وُحِّد **نموذج الإدخال**: `SmartFinanceForm` بثلاثة أوضاع يختارها المستخدم من داخل النموذج:

| الوضع | ماذا يفعل |
|---|---|
| «مصروف يومي» | `createExpense(isCapitalAsset=false)` |
| «شراء مواد» | `createPurchase` مع ربط اختياري بصنف كتالوج متتبَّع |
| «أصل للورشة» | `createExpense(isCapitalAsset=true)` + مودال إهلاك اختياري |

النموذج نفسه يُستعمل الآن في **كل** مداخل الإنشاء والتعديل، وحُذف نموذجان قديمان (939 سطراً). أُضيف سطر توضيحي يبيّن أثر كل وضع على الربح.

**لم يُوحَّد شيء آخر.** شاشة المالية ما تزال ثلاثة تبويبات: «مصاريفي · مشترياتي · مبيعاتي» (`FinanceClient.tsx` — ثابت `TABS`).

---

## 4. المشاكل المرصودة — تحقّق منها بنفسك

### أ. زرّان للإدخال يفتحان الشيء نفسه

- `ExpensesTab.tsx` → زر «إضافة مصروف جديد (+)»
- `PurchasesTab.tsx` → زر «تسجيل مشتريات جديدة (+)»
- كلاهما يفتح `SmartFinanceForm` نفسه.

**الادّعاء:** تسميتان مختلفتان لفعل واحد تُوهمان المستخدم أن عليه اختيار المدخل الصحيح قبل الضغط — بينما لا فرق بينهما.

⚠️ **تحقّق:** يوجد `FloatingActionButton` مستعمل في `FinanceClient.tsx` (~السطر 298) مع `addLabel[activeTab]`. افهم علاقته بأزرار التبويبات الداخلية — هل هي مسارات متوازية أم واحدة؟ هذا يغيّر التشخيص.

### ب. الأصول الرأسمالية تظهر داخل «المصاريف» و«المشتريات»

الأصل الرأسمالي يُخزَّن في `expense` أو `purchase` بعلم `isCapitalAsset=true`. القائمتان تعرضانه ضمن سجلّاتهما.

**الادّعاء:** الأصل الرأسمالي **ليس مصروفاً** — لا يُخصم من الربح التشغيلي (§2). عرضه ضمن المصاريف يجعل الرقم الذي يجمعه المالك بعينه غير مطابق لمصاريفه الفعلية.

### ج. هدر المخزون يظهر ضمن «المصاريف»

`adjustStock(direction='out')` يُنشئ صف `expense` بعلم `isInventoryWriteoff=true` **بلا حركة نقدية** (خسارة غير نقدية، INV-25). يظهر في القائمة بشارة «تلقائي» وللقراءة فقط.

**الادّعاء:** خسارة لم يُدفع فيها نقد لا تنتمي لقائمة «كم دفعت».

### د. صفحة `/assets` لا تعرض كل الأصول

`AssetsScreen` تستعمل `useCapitalAssets()` التي تقرأ جدول `capital_asset`. هذا الجدول يُملأ فقط عند تفعيل الإهلاك.

**الادّعاء:** أصل سُجِّل بلا إهلاك لا يظهر في `/assets` إطلاقاً — يظهر في المصاريف فقط. **تحقّق من هذا تحديداً، فهو أخطر البنود إن صحّ.**

---

## 5. الحلول المقترحة — قيّمها ولا تُسلّم بها

| # | المقترح | يحلّ |
|---|---|---|
| 1 | زرّ إدخال واحد (عائم) فوق كل التبويبات؛ التبويبات للعرض والبحث فقط | أ |
| 2 | تبويب رابع «الأصول» يقرأ من `expense`/`purchase` حيث `isCapitalAsset=true` (لا من `capital_asset`)، مع إبقاء `/assets` متخصّصة في جداول الإهلاك | ب، د |
| 3 | إخفاء صفوف الهدر افتراضياً من «المصاريف» + مفتاح «إظهار هدر المخزون» | ج |
| 4 | **رُفض:** دمج قائمتَي المصاريف والمشتريات في قائمة واحدة | — |

### حجّة رفض المقترح 4 (المقدَّمة للمالك — قيّمها بصرامة)

البطاقتان تعرضان حقولاً مختلفة:
- **مصروف:** الوصف، المبلغ، **الفئة**، التاريخ
- **شراء:** الصنف، الإجمالي، **المورّد**، **الكمية × سعر الوحدة**، التاريخ

وفلتر «الفئة» موجود في المصاريف فقط. الحجّة: قائمة موحَّدة إما تزدحم على 360px أو تُسقط معلومات.

⚠️ **المالك غير مقتنع بهذا الرفض تحديداً** — هو يريد تبويباً واحداً. **قيّم الحجّة بجدّية**: هل هي عذر تقني لتجنّب شغل أكبر، أم قيد تصميمي حقيقي؟ هل توجد أنماط واجهة تحلّ اختلاف الحقول (بطاقة متغيّرة الشكل حسب النوع، مجموعات زمنية، صفّ مضغوط + تفاصيل عند النقر)؟ لديك حرية كاملة في نقض هذا الرفض.

---

## 6. أسئلة أريد رأيك فيها صراحةً

1. **هل التشخيص صحيح؟** أي بند من (أ)–(د) مبالغ فيه أو غير حقيقي؟ وهل فاتَ شيء أهم؟
2. **تبويب واحد أم فصل؟** ما رأيك الصريح، ولماذا؟ إن رجّحت التبويب الواحد، **كيف تُحلّ مشكلة اختلاف الحقول على 360px عملياً؟**
3. **التسمية.** «حركاتي» مرفوضة. المالك اقترح إبقاء «مصاريف» وإدراج كل شيء تحتها — لكن الأصول الرأسمالية ليست مصروفاً محاسبياً. ما التسمية الصحيحة مهنياً وبالعربية؟ (التبويبات الحالية: «مصاريفي · مشترياتي · مبيعاتي» بياء الملكية.)
4. **الأصول.** تبويب رابع، أم `/assets` وحدها، أم شيء آخر؟ وكيف تُحلّ (د) إن صحّت؟
5. **الهدر.** إخفاء افتراضي بمفتاح؟ أم إبقاؤه؟ أم نقله للمخزون؟
6. **الترتيب والمخاطر.** ما الذي يُنفَّذ أولاً؟ أيّها يستحقّ التأجيل أو الإلغاء؟ أين ترى أعلى خطر على أرقام المالك أو على استقرار الشاشة؟
7. **بديل لم يُطرح.** إن كان لديك تصوّر مختلف كلياً أفضل من كل ما سبق — اطرحه. هذا مرحَّب به، وهو أهم ما قد تُقدّمه.

---

## 7. قيود يجب أن يحترمها أي حلّ تقترحه

1. **لا مساس بالمنطق المحاسبي.** `src/features/finance/pnl.ts` هو المصدر الوحيد لتعريف الربح (LOCKED-6 في §2). أي مقترح يغيّر حساباً مرفوض.
2. **لا migration ولا تغيير مخطط** — الحلّ في طبقة العرض. (إن رأيت أن الحلّ الصحيح يستلزم تغيير مخطط، قل ذلك صراحةً واشرح الكلفة — لكن اعلم أن المالك لا يملك مساراً سهلاً لتشغيل migrations على الإنتاج.)
3. **الهاتف أولاً:** 360px و390px، أهداف لمس ≥ 44px، لا تمرير أفقي.
4. **RTL:** خصائص منطقية فقط.
5. **لا أرقام مالية مُفبركة** في أي حال (§1 في `CLAUDE.md`).
6. **بيانات حيّة:** المالك يستخدم النظام منذ شهرين. أي تغيير يُبدّل ما يراه في قوائمه — انتبه لأثر ذلك على ثقته بالأرقام.

---

## 8. شكل التقرير المطلوب

بالعربية، ومباشر بلا مجاملة:

1. **تحقّق التشخيص** — بنداً بنداً (أ/ب/ج/د): مؤكَّد / مبالغ فيه / خاطئ، مع دليل من الكود (ملف:سطر).
2. **رأيك في كل مقترح** — أوافق / أعارض / أعدّل، والسبب.
3. **بديلك** إن وُجد.
4. **إجابات مباشرة** على الأسئلة السبعة في §6.
5. **ترتيب مقترح** مع تقدير المخاطر.
6. **ما لم تستطع التحقّق منه** ولماذا.

**كن صريحاً.** إن كانت التوصيات القائمة خاطئة أو زائدة أو ناقصة، فقل ذلك بوضوح — قيمة هذه الاستشارة كلّها في استقلال رأيك عنها. ولا تدّعِ تحقّقاً لم تقم به: إن لم تفحص ملفاً، قل إنك لم تفحصه.

**تذكير أخير: لا تُعدّل أي ملف. التقرير فقط.**
