# مهمة: تثبيت شريط المالية وإنهاء التمرير الأفقي

> إصلاح واجهة بحت. لا منطق محاسبي، لا قاعدة بيانات، **ولا نقل أي وظيفة خارج تبويبها**.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً** — §3 (حمايات مثبَّتة)، §10 (RTL والنقود)، §11 (بوابات الفحص).

المستخدم **شخص واحد** — مالك ورشة، ليس تقنياً، **على الهاتف 95% من الوقت** (360–390px).

### شكوى المالك

> «الأزرار التي تظهر في المالية والانتقالات بين التبويبات — التبويبان الرئيسيان يظهر ويختفي معهم أزرار أخرى.»

### الآلية المؤكَّدة من الكود

`PageToolbar.tsx:135` يستعمل `justify-center` للمجموعة الوسطى، وتوثيق المكوّن (~السطر 51) يقول: «الأزرار الغائبة لا تترك أثراً (لا حجز مساحة)».

النتيجة: **مركز الشريط يُعاد حسابه مع كل نقرة**. في «مبيعاتي» يُحقن زرّ فلتر (`FinanceClient.tsx:173`)، فتنزاح التبويبات نفسها جانباً. في «مدفوعاتي» يختفي فترتدّ. هذا ما يشعر به المالك «اهتزازاً».

هذه المهمة نتيجة **استشارتين مستقلّتين** اتّفقتا على التشخيص وعلى الحلّ أدناه.

---

## 🔴 القيد الأصرم — اقرأه قبل كل شيء

> **لا تنقل أي وظيفة موجودة اليوم داخل تبويب إلى خارجه.**

**ممنوع قطعياً:** نقل زرّ/فلتر/إجراء إلى قائمة «المزيد»، أو شريط جانبي، أو صفحة مستقلّة، أو خلف قائمة ثانوية تُخرجه من سياق التبويب. **ولا حذف أي وظيفة** بحجّة الازدحام.

**مسموح:** إعادة الترتيب داخل الشاشة، توزيع على سطرين، تغيير الشكل (منسدلة ↔ رقاقات)، تكبير أهداف اللمس، تغيير التسميات.

المالك يريد أدواته في مكان عمله. نُنظّم ما هو موجود **داخل** الشاشة — لا نُفرّغها بالنقل.

---

## الإصلاح 1 — إفراغ هيدر المالية من كل ما هو مشروط

### الحالة

`FinanceClient.tsx:173` — زرّ الفلتر مشروط بالتبويب:

```ts
const filters = activeTab === "sales" ? [{ key: "source", ... }]
              : activeTab === "accounts" ? [{ key: "type", ... }]
              : null;
```

### المطلوب

الهيدر في **كل** تبويبات المالية يحمل شيئين فقط: **التبويبات + البحث**. لا `filters` مُمرَّرة لـ`PageToolbar` من `FinanceClient` إطلاقاً.

الفلاتر تنتقل إلى **متن التبويب نفسه** (الإصلاحان 2 و3) — لا إلى خارجه.

**استثناء `accounts`:** هيدرها الخاص (العنوان + زرّ الرجوع) يبقى كما هو، لكن فلتر «النوع» ينتقل لمتن الشاشة رقاقاتٍ كبقيّة التبويبات.

**معيار القبول:** بدّل بين «مدفوعاتي» و«مبيعاتي» ذهاباً وإياباً — **موضع التبويبين لا يتغيّر بأي بكسل**.

---

## الإصلاح 2 — صفّ «مصاريف» على سطرين

### الحالة

`PaymentsTab.tsx:195` فيه `overflow-x-auto`. عند رقاقة «مصاريف» يحمل الصفّ الواحد **ستّة عناصر**: 4 رقاقات + منسدلة الفئات + زرّ «إدارة الفئات» — لا تتّسع على 360px، فيظهر تمرير أفقي و«كل الفئات» مقطوعاً.

**يخالف `CLAUDE.md` §10: لا تمرير أفقي.**

### المطلوب

```
سطر 1 (دائم):   الكل · مصاريف · مشتريات · أصول
سطر 2 (عند «مصاريف» فقط):   [▾ كل الفئات]      [إدارة الفئات]
```

- **احذف `overflow-x-auto`** من صفّ الرقاقات — الحلّ ألّا يزدحم، لا أن يُمرَّر.
- السطر الثاني بعرض كامل، والعنصران فيه ≥44px.
- **كلاهما يبقى داخل التبويب** — لا نقل ولا حذف.
- ظهور السطر الثاني يدفع القائمة لأسفل فقط، ولا يُزيح أي عنصر أفقياً.

**معيار القبول:** على 360px، في كل الرقاقات الأربع — لا تمرير أفقي ولا عنصر مقطوع.

---

## الإصلاح 3 — فلتر «مبيعاتي» رقاقات في المتن

### الحالة

`SALE_SOURCES` (`FinanceClient.tsx:56-60`) يُعرض كمنسدلة في الهيدر — بينما «مدفوعاتي» تستعمل رقاقات في المتن. شكلان لوظيفة واحدة.

### المطلوب

انقله إلى **متن `SalesTab`** رقاقاتٍ بنفس نمط `PaymentsTab`:

```
الكل · يدوي · طلب محوّل
```

**نقطة مُتحقَّق منها تُسهّل العمل:** `SalesTab.tsx:31` يقرأ `?source=` من الرابط بنفسه أصلاً. أعِد استعمال نفس المعامل — **لا تُغيّر منطق البيانات ولا `useInfiniteSales`**، فقط مصدر تغيير المعامل.

استخرج مكوّن رقاقات مشتركاً بين `PaymentsTab` و`SalesTab` إن كان ذلك يقلّل التكرار بلا تعقيد. إن رأيته يُعقّد أكثر مما يُفيد، اتركهما منفصلين وقل ذلك في تقريرك.

**معيار القبول:** `?source=manual` يعمل كما اليوم، والرقاقة النشطة تعكس قيمة الرابط.

---

## الإصلاح 4 — أهداف اللمس ≥44px

قاعدة `CLAUDE.md` §10. المخالفات المرصودة:

| الموضع | الحالي |
|---|---|
| `PaymentsTab.tsx:205` — الرقاقات | `min-h-[40px]` |
| `PaymentsTab.tsx:227` — منسدلة الفئات | `h-9 min-h-[36px]` |
| `PaymentsTab.tsx:243` — «إدارة الفئات» | `min-h-[36px]` |
| `AccountsTab.tsx:174,182` — «تحويل بيني» و«حساب جديد» | `min-h-[36px]` |

**ابحث عن غيرها** في نطاق المالية ورقاقات المبيعات الجديدة، وارفعها كلها لـ**44px**.

---

## الإصلاح 5 — عدّاد الحركات

سطر خفيف فوق القائمة يعرض **عدد السجلات المعروضة**:

> «14 حركة مسجّلة»

الفائدة: تأكيد فوري أن التصفية أثّرت.

### 🔴 عدد فقط — ولا مبلغ إطلاقاً

**ممنوع منعاً باتاً عرض أي مجموع مالي فوق القائمة.**

السبب: مجموع القائمة يخلط الرأسمالي والمخزون بالتشغيلي، فسيخالف حتماً رقم `computeOperatingPnl` في التقارير. النظام كلّه مبنيّ على **«رقم ربح واحد يثق به المالك»** (LOCKED-6، §2 في `CLAUDE.md`) — ورقم منافس يضرب ذلك مباشرة. الاستشارتان اتّفقتا على رفضه.

**نقطة تحتاج قراراً منك:** الترقيم يجلب صفحة صفحة، فالعدد المتاح هو «المحمَّل حتى الآن» لا الإجمالي. اختر أحد الأمرين وصرّح به:
- صياغة تعكس الواقع بلا كذب (مثل «14 حركة معروضة»)، أو
- عدّاد إجمالي حقيقي عبر استعلام `count` منفصل.

**لا تعرض رقماً جزئياً بصياغة توحي بأنه إجمالي.** إن اخترت الأول فالصياغة يجب أن تكون صادقة.

---

## 🔒 خطوط حمراء

1. **لا نقل وظيفة خارج تبويبها ولا حذفها** — الأهم (§القيد أعلاه).
2. **لا تلمس `src/features/finance/pnl.ts`** ولا أي منطق محاسبي.
3. **لا migration ولا تعديل مخطط.** `drizzle-kit generate` يبقى يطبع «No schema changes».
4. **لا تلمس `getPayments`** ولا استعلامات `queries.ts` — أُصلحت للتوّ بعد أعطال وقت تشغيل. هذه مهمة عرض بحتة.
5. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`text-start`/`text-end`. ممنوع `pl-`/`pr-`/`left-`/`right-`/`text-left`/`text-right`.
6. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ.
7. **صفوف الهدر تبقى للقراءة فقط** — بلا زرّ ⋯ (`isInventoryWriteoff`).
8. **لا تكسر التوافق العكسي:** `?tab=expenses` · `?tab=purchases` · `?tab=owner` · `?tab=opening` · `?source=` · `?filter=` · `?category=` · `?nature=`.
9. **لا تُغيّر `PageToolbar` نفسه** إلا إن ثبت أنه ضروري — سبع شاشات تستعمله. إن لزم، صرّح بالأثر على كلٍّ منها.

---

## بوابات الفحص — قبل كل كومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build                # البوابة الحقيقية
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **`typecheck` وحده لا يكفي** — لا يكشف خرق حدود الخادم/العميل. **`pnpm build` هو المعيار.**

⚠️ **وحتى `pnpm build` لا يكشف أخطاء وقت التشغيل.** جولة سابقة اجتازت البوابات الثلاث وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً**. إن لمست أي شيء يمسّ جلب البيانات، شغّله فعلياً.

---

## الاختبار المطلوب

نفّذ ما تستطيع، **وقل صراحةً ما لم تستطع ولماذا**. لا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.

1. **الثبات (الأهم):** بدّل بين «مدفوعاتي» و«مبيعاتي» مراراً — موضع التبويبين لا يتغيّر بأي بكسل.
2. **360px:** الرقاقات الأربع كلها — لا تمرير أفقي، لا عنصر مقطوع.
3. «مصاريف» تُظهر السطر الثاني كاملاً، والعودة لرقاقة أخرى تُخفيه بلا إزاحة أفقية.
4. فلتر المبيعات يعمل، و`?source=manual` يبقى صالحاً.
5. كل هدف لمس ≥44px.
6. العدّاد يتغيّر مع الفلتر، **وصياغته صادقة** بشأن المحمَّل مقابل الإجمالي.
7. التعديل والحذف يعملان للنوعين، وصفوف الهدر بلا زرّ ⋯.
8. كل معاملات §خطوط حمراء رقم 8 ما زالت تعمل.
9. `/reports` → «فحص الآن» → **الكل PASS أو WARN، ولا واحد FAIL**. إن ظهر `IC-1 = FAIL` **أوقف فوراً**.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.** المالك يراجع محلياً.
- كومت منفصل لكل إصلاح (1 / 2 / 3 / 4 / 5).
- رسائل بالعربية تشرح **لماذا** لا ماذا فقط.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. ما نُفِّذ وما لم يُنفَّذ **ولماذا**.
2. **مخرجات البوابات الثلاث حرفياً.**
3. أي اختبار من التسعة **لم** تنفّذه ولماذا.
4. قرارك في عدّاد الحركات (محمَّل أم إجمالي) وصياغته.
5. هل استخرجت مكوّن رقاقات مشتركاً أم لا، ولماذا.
6. أي خطّ أحمر اقتربت منه.

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود الفعلي.

**إن اكتشفت أن جزءاً من الخطة خاطئ أو يكسر شيئاً — توقّف وأبلغ بدل الالتفاف حوله.**
