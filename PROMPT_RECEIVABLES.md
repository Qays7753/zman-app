# مهمة: الذمم المدينة — تتبّع الديون النقدية بلا إفساد الربح

> ⚠️ **هذه المهمة مختلفة عن كل ما سبقها.** تمسّ **قاعدة البيانات** و**المعادلة المحاسبية**.
> القاعدة القديمة «لا تلمس المنطق المحاسبي» **لا تنطبق هنا** — لكن بحذر مضاعف وإثبات صريح.
>
> اقرأ الوثيقة كاملة قبل أي كود.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً — كلّه**، خصوصاً §2 (النموذج المالي)، §3 (حمايات مثبَّتة)، §6 (تطبيق قاعدة البيانات)، §10 (RTL والنقود)، §11 (بوابات الفحص).
**واقرأ `artifacts/zman-app/docs/ACCOUNTING_RULES.md`** — هو الدستور المحاسبي (INV-1..INV-25، IC-1..IC-14).

المستخدم **شخص واحد** — مالك ورشة، ليس محاسباً، **على الهاتف 95% من الوقت** (360–390px).

### الطلب بكلمات المالك

> «أنا عندي كاش لكن بعض المرات بعطي منهم دين لحدا، وهذول أنا ما بدي يكون مصاريف. بس بهمّني أعرف كم هم ولمين ومتى وكم ضلّ معي كاش، لأنه هذول مستردّات.»

### قيوده (مؤكَّدة منه مباشرة)

| # | القيد |
|---|---|
| 1 | **نقد فقط** — لا بيع بضاعة على الحساب. لا مساس بالمخزون ولا COGS |
| 2 | **لا تواريخ استحقاق ولا تذكيرات** — يكفي: كم، لمن، متى |
| 3 | **السداد على دفعات بمبالغ حرّة** — لا مبلغ ثابت ولا جدول أقساط |
| 4 | **الدَّين المسدَّد يبقى مسجَّلاً** للمراجعة، لا يُحذف |
| 5 | يريد رؤيتها **ضمن الشاشات المالية** لا في مكان منفصل |
| 6 | **لا توجد ديون قائمة اليوم** — تبدأ نظيفة، بلا تصحيح رجعي ولا تاريخ فاصل |

---

## 1. القاعدة المحاسبية — افهمها قبل أن تكتب سطراً

**الدَّين ليس مصروفاً. هو أصل (ذمّة مدينة).**

حين يُقرض المالك 50 ديناراً، لم يخسر شيئاً: تحوّل المال من «نقد» إلى «ذمّة عند فلان». ثروته لم تنقص — **تغيّر شكلها فقط**.

```
عند الإقراض:   نقد −50   ·   ذمم مدينة +50   ·   الربح: لا يتغيّر
عند السداد:    نقد +20   ·   ذمم مدينة −20   ·   الربح: لا يتغيّر
```

**تسجيله مصروفاً خطأ فادح:** يُنقص ربح شهر الإقراض بلا سبب، ثم يرفع ربح شهر السداد بلا سبب — وهو نقيض هدف المشروع كلّه («رقم ربح واحد يثق به المالك»).

### النمط المُحتذى: العربون

`deposit` يفعل الشيء نفسه بالاتجاه المعاكس، وهو **مبنيّ ويعمل**: نقد يدخل، لا يمسّ الربح، يظهر التزاماً في الميزانية. **ادرسه أولاً** — الذمم مرآته تماماً.

---

## 2. ما هو مبنيّ اليوم (مُتحقَّق منه)

- `cash_movement.source_type` مقيَّد بـ`CHECK` على ثمانية أنواع (`finance/db.ts` ~السطر 322):
  `sale · expense · purchase · deposit · owner_draw · owner_inject · opening · transfer`
  **لا شيء يعني «دَين»** — إضافة نوع تتطلّب migration.
- **لا يوجد** جدول ذمم ولا أي مفهوم دَين في المخطط.
- معادلة IC-1 في `reports/actions.ts` (~السطر 888):
  ```
  totalEquity = opening + injections − drawings + retainedProfit − capitalAdditions
  equityDriftCents = equityFromLedger − totalEquity   // يجب أن يبقى 0
  ```
- نمط جدول بسيط مماثل تحتذيه: `ownerTransaction` في `finance/db.ts`.

---

## 3. التصميم المطلوب

### 3.أ — جدولان (السداد الجزئي يفرض ذلك)

القيد 3 (دفعات بمبالغ حرّة) يجعل **جدولاً واحداً غير كافٍ**. المطلوب:

**`receivable`** — الدَّين نفسه:

| العمود | الغرض |
|---|---|
| `id` | uuid |
| `date` | تاريخ الإقراض |
| `person_name` | لمن (نصّ حرّ، `<= 200`) |
| `amount_cents` | المبلغ الأصلي (integer, `> 0`) |
| `account_id` | من أي صندوق خرج (FK) |
| `notes` | ملاحظة اختيارية (`<= 1000`) |
| `deleted_at` / `created_at` / `updated_at` | نمط المشروع |

**`receivable_payment`** — دفعات السداد:

| العمود | الغرض |
|---|---|
| `id` | uuid |
| `receivable_id` | FK → `receivable` (ON DELETE RESTRICT — حفاظاً على الأثر) |
| `date` | تاريخ الدفعة |
| `amount_cents` | مبلغ الدفعة (`> 0`) |
| `account_id` | لأي صندوق دخل (FK) |
| `deleted_at` / `created_at` / `updated_at` | نمط المشروع |

**الرصيد المتبقّي محسوب عند القراءة** (`amount_cents − Σ payments`) — **لا تُخزّنه عموداً**. تخزينه يخلق مصدر حقيقة ثانياً يمكن أن ينحرف.

**الحالة (قائم/مسدَّد) مشتقّة** من الرصيد، لا عمود. القيد 4 محفوظ تلقائياً: لا حذف، الصفّ يبقى برصيد صفر.

⚠️ **لا تسمح بدفعة تتجاوز المتبقّي.** تحقّق في الـ action ضمن نفس المعاملة.

### 3.ب — حركات الصندوق

- الإقراض → `cash_movement` بـ`direction='out'`, `source_type='receivable'`, `source_id=receivable.id`
- السداد → `cash_movement` بـ`direction='in'`, `source_type='receivable_payment'`, `source_id=payment.id`

**يتطلّب توسيع قيد `CHECK`** بنوعين جديدين — migration (§3.ج).

**كل عملية داخل معاملة واحدة** (`db.transaction`): الصفّ + حركة الصندوق معاً، أو لا شيء.

### 3.ج — الـ migration

ملف واحد `0027_receivables.sql` في `artifacts/zman-app/drizzle/migrations/`:
1. إنشاء الجدولين + الفهارس + قيود `CHECK`.
2. توسيع `cash_movement_source_type_enum` بـ`'receivable'` و`'receivable_payment'`.

**إلزامي:**
- **آمن للتشغيل المتكرر** (`IF NOT EXISTS` / `DO $$`) — يُشغَّل يدوياً ثلاث مرات بلا خطأ (§6 في `CLAUDE.md`).
- 🔴 **لا تُشغّله على قاعدة الإنتاج.** ولّد الملف فقط. المالك يُشغّله من SQL Editor. **لا تتّصل بقاعدة الإنتاج إطلاقاً.**
- توسيع الـ`CHECK` يحتاج `DROP CONSTRAINT` ثم `ADD` — اكتبه محروساً.
- بعد التوليد، `drizzle-kit generate` سيرى تغييراً في المخطط. هذا **متوقَّع ومقبول هنا فقط** (§بوابات الفحص).

### 3.د — المحاسبة: IC-1 يجب أن يبقى صفراً

هذا **أخطر جزء في المهمة**.

- **الربح لا يتأثّر إطلاقاً.** `computeOperatingPnl` في `pnl.ts`: أضف `source_type IN ('receivable','receivable_payment')` إلى **الاستثناءات** فلا تُحسب مصروفاً ولا إيراداً. لا تُغيّر أي بند قائم.
- **الميزانية:** الذمم المدينة **أصل**. في `getFinancialPosition`:
  - يُضاف `receivablesCents` (مجموع الأرصدة المتبقّية) إلى الأصول.
  - النقد نقص بنفس المقدار عند الإقراض.
  - فالمجموع لا يتغيّر و**`equityDriftCents` يبقى 0**.
- **لا تلمس تعريف الربح (LOCKED-6)** ولا `computeOperatingPnl` كمصدر وحيد. الإضافة استثناء لا تعريف جديد.
- **أضف فحص سلامة جديد** على نمط IC القائمة (مثلاً `IC-15`): *مجموع حركات الصندوق من نوع الذمم == مجموع (الديون − الدفعات)*. اتبع نمط `IC-13` القائم.

### 3.هـ — الواجهة

**1. الإدخال — وضع رابع في `SmartFinanceForm`** (`MODES` ~السطر 46):

```
[ 💵 مصروف يومي ]  [ 🛍️ شراء مواد ]  [ 🔧 أصل للورشة ]  [ 🤝 دَين لشخص ]
```

مع سطر توضيحي على نمط الأوضاع الثلاثة:
> «لا يُخصم من ربحك — مالك ما زال لك، لكن عند غيرك.»

⚠️ **أربعة أوضاع قد تزدحم على 360px.** افحص فعلياً. إن ازدحمت، وزّعها على سطرين — **ولا تُخفِ أي وضع خلف قائمة**.

الحقول: الاسم · المبلغ · التاريخ · الصندوق · ملاحظة (اختيارية).

**2. العرض — رقاقة خامسة في `PaymentsTab`** (`FILTER_CHIPS` ~السطر 41):

```
الكل · مصاريف · مشتريات · أصول · ديون
```

⚠️ خمس رقاقات + سطر الفئات قد تحتاج سطرين — `flex-wrap` قائم فعلاً. **لا تمرير أفقي** (قاعدة §10).

**بطاقة الدَّين تعرض:** الاسم · المبلغ الأصلي · **المتبقّي** · التاريخ · شارة «قائم» أو «مسدَّد».

**زرّ «تسجيل دفعة»** على الديون القائمة → مودال بمبلغ حرّ (≤ المتبقّي) وتاريخ وصندوق.

**سجلّ الدفعات مرئي** لكل دَين (القيد 4).

**3. رقاقة «الكل»:** الديون تظهر فيها كبقيّة الحركات، بشارة مميّزة تمنع الالتباس بمصروف.

**4. الميزانية في `/reports`:** سطر «ذمم مدينة» مع الأصول.

**لا تُضف شيئاً للوحة التحكم** في هذه الجولة — لم يقرّره المالك بعد.

---

## 🔒 خطوط حمراء

1. **لا تُشغّل أي migration على قاعدة الإنتاج.** ولّد الملف فقط. لا تتّصل بقاعدة إنتاج إطلاقاً.
2. **الدَّين لا يُخصم من الربح ولا يُضاف إليه.** لا عند الإقراض ولا عند السداد. أي مسار يجعله مصروفاً أو إيراداً **خطأ فادح**.
3. **`equityDriftCents` يجب أن يبقى 0** في كل السيناريوهات. إن ظهر `IC-1 = FAIL` — **توقّف فوراً وأبلغ، ولا تكوّت**.
4. **لا تُغيّر تعريف الربح** (LOCKED-6, §2). `computeOperatingPnl` يبقى المصدر الوحيد.
5. **لا تلمس** `getPayments` ولا استعلامات `queries.ts` القائمة إلا بإضافة مسار الذمم — أُصلحت للتوّ بعد أعطال وقت تشغيل.
6. **`purchase.total_cents` عمود `GENERATED ALWAYS`** — لا يُكتب يدوياً.
7. **النقود أعداد صحيحة بالفلس.** `formatFilsToJod` هو المنسّق الوحيد. **لا حساب عائم إطلاقاً.**
8. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`text-start`/`text-end`. ممنوع `pl-`/`pr-`/`left-`/`right-`.
9. **أهداف اللمس ≥ 44px**، 360px و390px، **لا تمرير أفقي**.
10. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ (§1 في `CLAUDE.md`).
11. **لا حذف صلب** لأي دَين أو دفعة — `deleted_at` فقط، والمسدَّد يبقى ظاهراً (القيد 4).
12. **لا تنقل أي وظيفة قائمة خارج تبويبها.**

---

## بوابات الفحص

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build                # البوابة الحقيقية
pnpm typecheck
pnpm drizzle-kit generate
```

⚠️ **استثناء لهذه المهمة وحدها:** `drizzle-kit generate` **سيُظهر تغييراً في المخطط** — هذا متوقَّع (جدولان جديدان). تأكّد أن التغيير **مطابق تماماً** لما في `0027_receivables.sql` ولا شيء غيره. أي تغيير إضافي غير مقصود = خطأ.

⚠️ **`pnpm build` لا يكشف أخطاء وقت التشغيل.** جولة سابقة اجتازت البوابات الثلاث وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً** (خطأ SQL في `UNION ALL`). **إن كتبت استعلاماً جديداً، لا تفترض صحّته من البناء.**

---

## الاختبار المطلوب

نفّذ ما تستطيع. **قل صراحةً ما لم تستطع ولماذا — ولا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.**

1. تسجيل دَين → النقد ينقص، **الربح لا يتغيّر إطلاقاً**، يظهر في «ديون».
2. دفعة جزئية → النقد يزيد، **الربح لا يتغيّر**، المتبقّي ينقص بالضبط.
3. دفعات متعدّدة حتى السداد الكامل → المتبقّي = 0، الحالة «مسدَّد»، **الصفّ يبقى ظاهراً**.
4. محاولة دفعة تتجاوز المتبقّي → **تُرفض برسالة واضحة**.
5. الميزانية: «ذمم مدينة» = مجموع المتبقّي، و**`equityDrift = 0`**.
6. `/reports` → «فحص الآن» → **الكل PASS أو WARN، ولا واحد FAIL** — خصوصاً `IC-1` وفحصك الجديد.
7. 360px: أربعة أوضاع وخمس رقاقات — لا تمرير أفقي، أهداف ≥44px.
8. الرقاقات الأربع القديمة والبحث والترقيم ما زالت تعمل.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.**
- كومت منفصل لكل مرحلة منطقية (مخطط+migration / محاسبة / واجهة).
- رسائل بالعربية تشرح **لماذا**.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. ما نُفِّذ وما لم يُنفَّذ **ولماذا**.
2. **مخرجات البوابات الثلاث حرفياً** — وتفسير ما أظهره `drizzle-kit generate` بالضبط.
3. **كيف ضمنت أن `equityDrift = 0`** — اشرح المعادلة بعد إضافة الذمم.
4. الفحص الجديد الذي أضفته ورقمه ومنطقه.
5. أي اختبار من الثمانية **لم** تنفّذه ولماذا.
6. أي خطّ أحمر اقتربت منه.
7. **ما يجب أن يفعله المالك يدوياً** — تعليمات تشغيل الـ migration خطوة بخطوة.

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود، **وسيُختبر الأثر المحاسبي على قاعدة بيانات فعلية قبل أي رفع.**

**إن اكتشفت أن جزءاً من التصميم خاطئ أو يكسر IC-1 — توقّف وأبلغ فوراً بدل الالتفاف حوله.**
