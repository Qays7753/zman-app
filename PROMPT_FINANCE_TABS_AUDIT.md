# مهمة: تدقيق وإصلاح أزرار شاشة المالية والانتقال بين التبويبات

> **الطبيعة:** تحقيق أولاً، ثم إصلاح. لا تبدأ بالكود — ابدأ بالفحص وبناء خريطة كاملة للسلوك الحالي.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في جذر المستودع أولاً** — §3 (حمايات مثبَّتة)، §10 (RTL والنقود، والمستخدم على الهاتف 95%)، §11 (بوابات الفحص).

المستخدم **شخص واحد** — مالك ورشة، ليس تقنياً، **على الهاتف 95% من الوقت** (360–390px).

### شكوى المالك

> «الأزرار التي تظهر في المالية والانتقالات بين التبويبات — التبويبان الرئيسيان يظهر ويختفي معهم أزرار أخرى.»

الملف المحوري: `src/app/(app)/finance/FinanceClient.tsx`.

### خلفية ضرورية

في جولة سابقة (مدموجة ومرفوعة) وُحِّدت شاشة المالية: كان فيها ثلاثة تبويبات ظاهرة («مصاريفي · مشترياتي · مبيعاتي») فصارت **تبويبين** («مدفوعاتي · مبيعاتي») مع دمج المصاريف والمشتريات في `PaymentsTab` واحد وفلتر رقاقات (الكل · مصاريف · مشتريات · أصول).

**الشكوى الحالية على الأرجح أثر جانبي لتلك الجولة.** لكن **لا تفترض** — تحقّق بنفسك، فقد تكون بعض المشكلات أقدم منها.

---

## 1. مشكلات مرصودة مبدئياً — تحقّق من كلٍّ منها ولا تُسلّم بها

هذه ما رصدته مراجعة أولية على الكود. **اعتبرها فرضيات**: أكّدها أو انفِها بدليل (ملف:سطر)، وابحث عن غيرها.

### م-1 — تبويبات تعمل لكنها غير ظاهرة في الشريط

`TABS` في `FinanceClient.tsx` (~السطر 77) يحوي **عنصرين فقط**: `payments` و`sales`.

لكن الشيفرة تتعامل مع تبويبات أخرى فعّالة: `accounts`, `owner`, `opening` — لها فلاتر و`addLabel` و`paramMap` وشرط عرض (`activeTab === "accounts"` ~السطر 291).

يُوصَل إليها عبر روابط خارجية فقط (`nav.ts` وقائمة «المزيد» و`DashboardClient.tsx:532-535`).

**افحص:** حين يفتح المالك `/finance?tab=accounts`:
- ماذا يعرض شريط التبويبات؟ هل يبدو **أن لا تبويب نشطاً** (لأن `accounts` ليست في `TABS`)؟
- هل يستطيع العودة لـ«مدفوعاتي» من داخل الشاشة، أم يحتاج زرّ الرجوع في المتصفّح؟
- ما شكل الشريط بصرياً في هذه الحالة؟

**هذا أرجح تفسير لشكوى «يظهر ويختفي».**

### م-2 — تبويب `owner` بلا مسار عرض

`paramMap` (~السطر 216) و`addLabel` (~السطر 231) يعرّفان `owner`، لكن **لا يوجد شرط عرض** `activeTab === "owner"` في منطقة التقديم (~السطر 288-296). `OwnerTab` يُعرض داخل شرط `accounts` فقط.

**افحص:** ماذا يحدث فعلياً عند فتح `/finance?tab=owner` — وهو رابط مستعمل في `DashboardClient.tsx:534`؟ هل تظهر شاشة فارغة؟ وهل الزرّ العائم يعمل ويفتح مودالاً لا يراه المستخدم؟

### م-3 — الزرّ العائم يعمل في تبويب `opening`

`isActionableTab = activeTab !== "opening"` (~السطر 166) يُخفي الزرّ في `opening` فقط.

**افحص:** في `owner` (إن كان يعرض شيئاً) وفي أي تبويب آخر — هل الزرّ العائم يظهر دائماً؟ وهل `paramMap[activeTab]` يُرجع `undefined` فيصير الزرّ بلا وظيفة (`if (!queryParam) return;`)؟ **زرّ ظاهر لا يفعل شيئاً عند الضغط هو أسوأ من زرّ غائب.**

### م-4 — فلتر «الفئة» يختفي ويظهر مع الرقاقة

الفلتر مشروط بـ `activeTab === "payments" && currentFilter === "expense"` (~السطر 171).

**افحص:** حين يبدّل المالك الرقاقات (الكل → مصاريف → مشتريات)، هل يظهر الفلتر ويختفي فيقفز الشريط ويتغيّر ارتفاعه؟ **على 360px هذا اضطراب بصري ملموس.** وهل قيمة `?category=` تبقى في الرابط بعد الانتقال لرقاقة أخرى فتُطبَّق تصفية خفية غير مرئية؟

---

## 2. المطلوب — المرحلة أ: تحقيق (لا كود بعد)

ابنِ **خريطة كاملة** لسلوك شاشة المالية، ووثّقها في تقريرك:

1. **جدول لكل تبويب** (`payments`, `sales`, `accounts`, `owner`, `opening`):

| التبويب | ظاهر في الشريط؟ | كيف يُوصَل إليه؟ | يعرض محتوى؟ | الزرّ العائم: يظهر؟ ويعمل؟ | الفلاتر الظاهرة | البحث يعمل؟ | كيف يعود المستخدم؟ |
|---|---|---|---|---|---|---|---|

2. **كل زرّ في الشاشة**: أين يظهر، ومتى يختفي، وماذا يفعل، وهل هناك حالة يظهر فيها بلا وظيفة.

3. **حالات الانتقال:** ماذا يحدث لمعاملات الرابط (`?filter=`, `?category=`, `?search=`) عند التنقّل بين التبويبات والرقاقات؟ هل تتسرّب قيمة من سياق لآخر؟

4. **كل مدخل خارجي** لشاشة المالية — ابحث عن كل رابط `/finance...` في الكود (`nav.ts`, `DashboardClient.tsx`, `GlobalSearch.tsx`, `activities/page.tsx`, وأي مكان آخر) وتحقّق أن كلاً منها يصل لوجهة صحيحة تعرض محتوى.

**افحص على 360px تحديداً**: هل يتّسع الشريط؟ هل يقفز ارتفاعه عند ظهور/اختفاء الفلاتر؟ هل يوجد تمرير أفقي؟

---

## 3. المطلوب — المرحلة ب: الإصلاح

بعد اكتمال الخريطة، أصلح ما وجدته وفق هذه المبادئ:

1. **لا زرّ ظاهر بلا وظيفة.** إن لم يكن للزرّ فعل في السياق الحالي، يُخفى — لا يُترك ظاهراً ولا يستجيب.
2. **لا تبويب فعّال بلا مسار عودة.** أي شاشة يصلها المالك يجب أن يستطيع مغادرتها من داخل الواجهة، بلا زرّ رجوع المتصفّح.
3. **لا قفز في التخطيط.** ظهور/اختفاء الفلاتر يجب ألّا يُغيّر ارتفاع الشريط بشكل مزعج على 360px.
4. **لا حالة «لا شيء نشط».** إن كان المالك في تبويب غير معروض في الشريط، يجب أن يفهم أين هو وكيف يعود.
5. **لا معاملات رابط متسرّبة.** فلتر خاص بسياق يُنظَّف عند مغادرته.

**قرارات التصميم متروكة لك** — أنت أدرى بعد التحقيق. لكن **إن كان الإصلاح يتطلّب تغييراً بنيوياً كبيراً** (مثل إعادة إظهار تبويبات محذوفة، أو تغيير بنية التنقّل، أو نقل شاشة لمسار مستقلّ)، **توقّف واعرض الخيارات في تقريرك بدل التنفيذ** — المالك يقرّر.

نفّذ بلا تردّد الإصلاحات الصغيرة الواضحة (زرّ بلا وظيفة، شرط عرض ناقص، معامل متسرّب).

---

## 🔒 خطوط حمراء

1. **لا تلمس `src/features/finance/pnl.ts`** ولا أي منطق محاسبي. هذه مهمة تنقّل وواجهة بحتة.
2. **لا migration ولا تعديل مخطط.** `pnpm drizzle-kit generate` يجب أن يبقى يطبع «No schema changes, nothing to migrate 😴».
3. **لا تكسر التوافق العكسي.** `?tab=expenses` و`?tab=purchases` يجب أن يبقيا يوجّهان لـ`payments` مع الرقاقة الصحيحة (منطق قائم في `FinanceClient.tsx:93-105`).
4. **لا تكسر أي رابط خارجي** — كل روابط `DashboardClient` و`nav.ts` و`GlobalSearch` و`activities` يجب أن تبقى تعمل.
5. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`text-start`/`text-end`. ممنوع `pl-`/`pr-`/`left-`/`right-`/`text-left`/`text-right`.
6. **أهداف اللمس ≥ 44px**، ويُفحص على 360px و390px: لا تمرير أفقي، لا قصّ، لا تراكب.
7. **لا تُعِد `ExpensesTab` أو `PurchasesTab`** — حُذفا عمداً بعد الدمج.
8. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ (§1 في `CLAUDE.md`).

---

## بوابات الفحص — إلزامية قبل كل كومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build                # البوابة الحقيقية
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **`typecheck` وحده لا يكفي** — لا يكشف خرق حدود الخادم/العميل. **`pnpm build` هو المعيار.**

⚠️ لا تشغّل `pnpm build` من جذر الـ monorepo — يفشل على حزمة غير ذات صلة.

---

## الاختبار المطلوب

نفّذ ما تستطيع، **وقل صراحةً ما لم تستطع ولماذا** (لا تعلّم ✅ على اختبار استنتجته من الكود بدل تنفيذه):

1. كل تبويب في الجدول: افتحه بالرابط المباشر وتأكّد أنه يعرض محتوى وله مسار عودة.
2. كل رابط خارجي لشاشة المالية يصل لوجهة صحيحة.
3. الزرّ العائم: يظهر فقط حيث له وظيفة، ويعمل فعلاً حيث يظهر.
4. التنقّل بين الرقاقات: لا تسرّب معاملات، لا تصفية خفية.
5. `?tab=expenses` و`?tab=purchases` ما زالا يعملان.
6. 360px: لا تمرير أفقي، لا قفز مزعج في ارتفاع الشريط، أهداف اللمس ≥ 44px.
7. `/reports` → «فحص الآن» → **الكل PASS أو WARN، ولا واحد FAIL**. إن ظهر `IC-1 = FAIL` **أوقف فوراً**.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.** المالك يراجع محلياً.
- كومت منفصل لكل إصلاح منطقي — للمراجعة والتراجع.
- رسائل الكومت بالعربية، تشرح **لماذا** لا ماذا فقط.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. **خريطة السلوك الكاملة** (الجداول المطلوبة في §2) — هذا أهم مخرَج، حتى لو لم تُصلح شيئاً.
2. **حكمك على م-1 إلى م-4**: مؤكَّدة / مبالغ فيها / خاطئة، بدليل (ملف:سطر).
3. **ما وجدته ولم يُذكر** في هذه الوثيقة.
4. **ما أصلحته وكيف**، وما تركته ولماذا.
5. **الخيارات المعروضة على المالك** إن وُجد إصلاح بنيوي كبير.
6. **مخرجات البوابات الثلاث حرفياً**.
7. **أي اختبار من السبعة لم تنفّذه ولماذا.**

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود الفعلي. وإن استنتجت نتيجة اختبار من قراءة الكود بدل تشغيله، **قل ذلك صراحةً** — الاستنتاج مقبول، وادّعاء التنفيذ ليس كذلك.

**إن اكتشفت أثناء العمل أن جزءاً من التشخيص خاطئ — توقّف وأبلغ بدل الالتفاف حوله.**
