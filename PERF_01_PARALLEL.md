# المرحلة 1 من 4 — توازي الاستعلامات المالية

> ⚠️ **نفّذ هذه المرحلة فقط. توقّف عند نهايتها وانتظر إذن المالك قبل أي شيء آخر.**
> هناك ثلاث مراحل تالية — **لا تبدأها ولا تُلمّح إليها في الكود.**

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً — كلّه**، خصوصاً §2 (النموذج المالي وLOCKED-6)، §3، §11.

المستخدم **شخص واحد** — مالك ورشة، على الهاتف 95% من الوقت.

### الشكوى

> «النظام بطيء، وخاصة عند فتحه. وأحياناً يعطي: حدث خطأ أثناء تحميل البيانات.»

### التشخيص — مقيس فعلياً على قاعدة الإنتاج

| القياس | القيمة |
|---|---|
| زمن الشبكة لكل استعلام (RTT) | **~150 ms** |
| `computeOperatingPnl` (استدعاء واحد) | **~1,560 ms** |
| `getMonthlyProfit` (6 أشهر) | **~10,755 ms** |
| نفس الستّة بالتوازي | **~3,211 ms** |

**حجم البيانات: 83 حركة صندوق · 16 مصروفاً · 37 شراءً · 27 مبيعة.**

### 🔴 الاستنتاج الحاسم — افهمه قبل أن تكتب سطراً

قاعدة البيانات **لا تحسب شيئاً**. `select 1` التافه يستغرق 150ms. الخادم يعالج في أقلّ من مللي ثانية؛ **الباقي كلّه شبكة**.

> **المشكلة ليست استعلامات بطيئة — بل عدد الرحلات المتسلسلة.**

**لذلك:** لا تُضِف فهارس، ولا تُحسّن استعلامات، ولا تُخزّن نتائج محسوبة. بـ83 صفاً هذا بلا فائدة، والأخير يخلق مصدر حقيقة ثانياً يخالف LOCKED-6.

---

## 🔒 الخطّ الأحمر الأول — الأرقام المالية

`pnl.ts` محروس بـ**LOCKED-6**: تعريف الربح يجب أن يتطابق في ثلاثة مواضع (لوحة التحكم · التقارير · الرسم الشهري)، و`IC-13` يفحصه وقت التشغيل.

**إلزامي:**
1. **سجّل الأرقام قبل التغيير** — الربح، النقد، إجمالي الأصول، `equityDriftCents`.
2. نفّذ التغيير.
3. **سجّلها بعده وأثبت التطابق بالفلس.**
4. شغّل فحص السلامة → **صفر FAIL**.

**إن اختلف رقم واحد — تراجع فوراً وأبلغ. لا تُكوّت.**

ℹ️ `IC-12 = WARN` (أرصدة مخزون سالبة) **موجود مسبقاً** — ليس من عملك، تجاهله.

---

## المطلوب في هذه المرحلة

### 1.أ — `computeOperatingPnl` (`src/features/finance/pnl.ts`)

ستّة استعلامات **متسلسلة** (الأسطر ~141، 162، 204، 257، 305، 339) وصفر `Promise.all`. كلها **قراءة مستقلّة** — لا يعتمد أيّها على نتيجة الآخر.

**المطلوب:** اجمعها في `Promise.all` واحد.

⚠️ **تحقّق بنفسك من الاستقلال قبل التوازي.** إن وجدت استعلاماً يعتمد على سابقه، **اتركه متسلسلاً وقل ذلك في تقريرك** — لا تُخاطر بالأرقام لأجل السرعة.
⚠️ `getDepreciationForPeriodCents` يُستدعى ضمنها — تأكّد أنه آمن للتوازي.

**المتوقّع:** ~1,560ms → ~250ms.

### 1.ب — `getMonthlyProfit` (`src/features/dashboard/queries.ts` ~679)

حلقة `for` تنتظر كل شهر قبل التالي. الأشهر مستقلّة تماماً.

**المطلوب:** `Promise.all` على الأشهر الستّة، **مع الحفاظ على ترتيبها في النتيجة**.

**المتوقّع:** ~10,755ms → ~1,500ms (وأقلّ بعد 1.أ).

### 1.ج — `src/features/finance/queries.ts`

فيه 15 `await` متتالياً وصفر `Promise.all`. **افحص كل دالة**: ما كان مستقلّاً وازِه، وما كان متعاقباً اتركه كما هو.

---

## 🔒 خطوط حمراء

1. **الأرقام المالية لا تتغيّر بفلس.** أثبت ذلك قبل/بعد.
2. **لا تُغيّر تعريف الربح** (LOCKED-6). التوازي لا يُغيّر منطقاً — إن اضطررت لتغيير منطق، **توقّف وأبلغ**.
3. **لا migration ولا تعديل مخطط.** `drizzle-kit generate` يبقى يطبع «No schema changes».
4. **لا تُشغّل شيئاً على قاعدة الإنتاج** — لا سكربت ولا SQL.
5. **لا فهارس جديدة ولا نتائج محسوبة مخزَّنة.**
6. **لا تلمس** `getPayments` ولا `getReceivables` — أُصلحتا مؤخّراً بعد أعطال وقت تشغيل.
7. **لا تلمس الواجهة** — هذه مرحلة خادم بحتة. لا مكوّنات، لا هوكس، لا CSS.
8. **لا تلمس** `lib/db/client.ts` ولا `providers/query-provider.tsx` — **مرحلة تالية**.
9. **لا تحذف دالة** قبل التحقّق بـgrep أنها بلا مستدعٍ.

---

## بوابات الفحص — قبل الكومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **البوابات الثلاث لا تكشف أخطاء وقت التشغيل.** جولة سابقة اجتازتها كلها وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً** (خطأ SQL). **لا تفترض صحّة شيء من البناء.**

---

## القياس المطلوب — إلزامي

**هذه مهمة أداء: بلا أرقام قبل/بعد لا قيمة لعملك.**

شغّل الدوال فعلياً وقِس. **أحمِ قبل القياس** — أول استدعاء يحمل كلفة الاتصال البارد (~1,320ms) ويُفسد النتيجة.

| # | القياس | قبل (مقيس) | بعد |
|---|---|---|---|
| 1 | `computeOperatingPnl` استدعاء واحد | ~1,560 ms | ؟ |
| 2 | `getMonthlyProfit` | ~10,755 ms | ؟ |

---

## الاختبار المطلوب

**قل صراحةً ما لم تنفّذه ولماذا. لا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.**

1. **الأرقام المالية متطابقة بالفلس** قبل/بعد — الربح، النقد، الأصول، `equityDrift`.
2. فحص السلامة → **صفر FAIL** (`IC-12 = WARN` مقبول ومسبق).
3. لوحة التحكم والتقارير تعرضان نفس الأرقام تماماً.

---

## الكومت

- **لا ترفع إلى أي مستودع بعيد.**
- كومت واحد لهذه المرحلة.
- رسالة بالعربية تشرح **لماذا**، مع **أرقام القياس**.
- ذيّلها بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. **جدول قبل/بعد بالأرقام المقيسة** — أهمّ مخرَج.
2. **إثبات تطابق الأرقام المالية بالفلس.**
3. مخرجات البوابات الثلاث **حرفياً**.
4. **أي استعلام تركته متسلسلاً ولماذا** (§1.أ).
5. ما لم تنفّذه ولماذا.

---

## 🛑 عند الانتهاء

**توقّف. لا تبدأ أي عمل آخر.**

اكتب في نهاية تقريرك: **«المرحلة 1 اكتملت — بانتظار إذن المالك للمرحلة 2.»**

**إن اكتشفت أن جزءاً من الخطة خاطئ أو يُهدّد الأرقام — توقّف وأبلغ فوراً بدل الالتفاف حوله.**
