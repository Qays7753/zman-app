# المرحلة 4 من 4 — الشاشات المهمَلة وN+1 والإحماء

> ⚠️ **المرحلة الأخيرة. نفّذها ثم توقّف.**
> **المراحل 1 و2 و3 أُنجزت واعتُمدت** — لا تُعِد عملها.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً**، خصوصاً §3 (حمايات مثبَّتة)، §5 (المشاكل المعروفة — **م-9**)، §10، §11.

المستخدم **شخص واحد** — مالك ورشة، على الهاتف 95% من الوقت.

### التشخيص — مقيس فعلياً

| القياس | القيمة |
|---|---|
| زمن الشبكة لكل رحلة (RTT) | **~150 ms** |
| الاتصال البارد الأول | **~1,320 ms** |
| استدعاءات هوكس في `CatalogClient` | **~21** |
| استدعاءات هوكس في `OrdersClient` | **~15** |

**المبدأ نفسه:** قاعدة البيانات لا تحسب شيئاً — **كل رحلة من المتصفّح تكلّف ~150ms**. المطلوب تقليل عدد الرحلات.

---

## المطلوب في هذه المرحلة

### 4.أ — `useComponentStock` — نمط N+1 مؤكَّد 🔴

`src/app/(app)/catalog/CatalogClient.tsx:362` **داخل بطاقة صنف**:

```tsx
const { data: stock } = useComponentStock(item.tracked ? item.id : undefined);
```

**استعلام مستقلّ لكل صنف متتبَّع.** عشرون صنفاً = **عشرون رحلة شبكة**.

موثَّق في `CLAUDE.md` كـ**م-9**.

**المطلوب:** استعلام واحد يجلب أرصدة **كل** الأصناف دفعة واحدة، والبطاقات تقرأ منه.

⚠️ **`useComponentStock` يُستعمل في مواضع أخرى** — `CatalogClient.tsx:467` و`:766`، ومحرّر المكوّنات، ونموذج الطلب. **افحص كل المستدعين بـgrep قبل التغيير.** إن كان موضع يحتاج صنفاً واحداً فقط، **أبقِ الهوك الأصلي له** ولا تكسره.

### 4.ب — الكتالوج والطلبات

**قِس أولاً، ثم قرّر.**

`CatalogClient` (~21 هوك) و`OrdersClient` (~15). طبّق نمط الحزمة (من المرحلة 3) **إن ثبتت الفائدة بالقياس**.

⚠️ **لا تُعمّم بلا قياس.** إن وجدت أن كثيراً من الاستدعاءات **شرطية** (لا تعمل إلا عند فتح مودال) أو **خفيفة**، فالحزمة قد تضرّ لا تنفع — **قل ذلك ولا تُغيّر**.

هذا قرار هندسي متروك لك بعد القياس، وأريد تبريره في التقرير.

### 4.ج — إحماء يمنع سبات Supabase

**غير موجود اليوم.** الاتصال البارد يكلّف ~1,320ms — وهو سبب «بطء أول فتحة في اليوم».

**المطلوب:** مسار `GET /api/health` خفيف يُنفّذ `select 1` **فقط**.

**متطلّبات:**
- **لا يقرأ بيانات ولا يُرجع أرقاماً** — `select 1` وحالة 200 فقط.
- **لا يتطلّب مصادقة**؟ ⚠️ **تحقّق من `middleware.ts`** — التطبيق محمي بـ`PASSCODE` ويُعيد التوجيه لتسجيل الدخول. المسار يجب أن يكون **مستثنى** ليعمل الـcron، **لكن لا تكشف أي بيانات عبره**.

🔴 **لا تُنشئ `vercel.json`** — ولا أي ملف إعداد خاصّ بمنصّة نشر.

المشروع **لا يحوي `vercel.json`** — Vercel مُعدّ عبر Dashboard (§10 في `CLAUDE.md`). إنشاء الملف **قد يُبطل إعدادات Dashboard ويكسر النشر كلياً**.

⚠️ **والمالك يخطّط للانتقال إلى Cloudflare مستقبلاً.** لذلك:
- **اجعل المسار محايد المنصّة تماماً** — Route Handler عادي في Next.js، بلا أي اعتماد على Vercel.
- **لا تكتب كوداً خاصاً بمنصّة** ولا تفترض بيئة تشغيل بعينها.
- **لا تُعِدّ المُجدوِل نفسه إطلاقاً.**

**أنشئ المسار فقط**، واكتب في تقريرك **تعليمات الجدولة اليدوية** — بديلين مختصرين: Vercel Cron من Dashboard، وCloudflare Cron Triggers. القرار للمالك.

**إن رأيت أي ملف إعداد منصّة ضرورياً — توقّف وأبلغ، ولا تُنشئه.**

---

## 🔒 خطوط حمراء

1. **لا تلمس أي منطق محاسبي** — لا `pnl.ts`، لا `reports/actions.ts`، لا `integrityCheck.ts`.
2. **لا `vercel.json`** (§4.ج) — الأهمّ في هذه المرحلة.
3. **لا migration ولا تعديل مخطط.** `drizzle-kit generate` يبقى يطبع «No schema changes».
4. **لا تُشغّل شيئاً على قاعدة الإنتاج.**
5. **لا تلمس** `getPayments` ولا `getReceivables` — أُصلحتا مؤخّراً بعد أعطال وقت تشغيل.
6. **لا تلمس** لوحة التحكم ولا حزمتها — أُنجزت في المرحلة 3.
7. **لا تلمس** `lib/db/client.ts` ولا `query-provider.tsx` — أُنجزا في المرحلة 2.
8. **لا تكسر أي مستدعٍ لـ`useComponentStock`** — افحصهم كلهم أولاً.
9. **لا أرقام مالية مُفبركة** ولا أرصدة افتراضية عند غياب البيانات — رصيد مخزون خاطئ يُفسد قرار المالك.
10. **لا تحذف دالة** قبل التحقّق بـgrep أنها بلا مستدعٍ.
11. **RTL — خصائص منطقية فقط.** أهداف اللمس ≥44px. لا تمرير أفقي.
12. **لا تكسر التوافق العكسي** لأي معامل رابط.

---

## بوابات الفحص — قبل الكومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **البوابات الثلاث لا تكشف أخطاء وقت التشغيل.** جولة سابقة اجتازتها كلها وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً**. **شغّل ما تكتبه فعلياً.**

---

## القياس المطلوب

⚠️ **مؤكَّد من المراحل الثلاث السابقة: بيئتك لا تملك `DATABASE_URL`** — ملف `.env` في جذر المستودع لا في `artifacts/zman-app`. **لا تُضيّع الوقت في محاولات اتصال.**

قل صراحةً: «تعذّر القياس — لا وصول لقاعدة البيانات». **المالك سيقيس ويتحقّق قبل الرفع.**

**ما يلزمك بدلاً منه — عدّ الرحلات لا زمنها:**

| # | المطلوب |
|---|---|
| 1 | عدد استعلامات المخزون في الكتالوج قبل/بعد (بدلالة عدد الأصناف المتتبَّعة) |
| 2 | عدد استدعاءات الهوكس في `OrdersClient` قبل/بعد |
| 3 | **أثبت بالكود** أن أرصدة المخزون المعروضة هي نفسها — لا حساب جديد |

---

## الاختبار المطلوب

**قل صراحةً ما لم تنفّذه ولماذا. لا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.**

1. **الكتالوج يعرض أرصدة صحيحة** لكل صنف متتبَّع — قارنها بالقيم قبل التغيير.
2. **كل مستدعٍ آخر لـ`useComponentStock` ما زال يعمل** (محرّر المكوّنات، نموذج الطلب).
3. الطلبات تعمل كما كانت.
4. `/api/health` يُرجع 200 بلا أي بيانات، **ويعمل بلا تسجيل دخول**.
5. **المسار لا يكشف شيئاً** — لا أرقام، لا أسماء، لا حالة قاعدة بيانات تفصيلية.
6. فحص السلامة → **صفر FAIL** (`IC-12 = WARN` مقبول ومسبق).
7. 360px: لا تمرير أفقي، لا كسر في التخطيط.

---

## الكومت

- **لا ترفع إلى أي مستودع بعيد.**
- كومت منفصل لكل بند (4.أ / 4.ب / 4.ج).
- رسائل بالعربية تشرح **لماذا**، مع **أرقام القياس**.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. **جدول قبل/بعد بالأرقام المقيسة.**
2. **كل مستدعٍ لـ`useComponentStock`** وكيف عالجته.
3. **نتيجة قياس الكتالوج والطلبات** — وهل عمّمت الحزمة، ولماذا (§4.ب).
4. **تعليمات إعداد Vercel Cron يدوياً** للمالك — خطوة بخطوة.
5. كيف حميت `/api/health` من كشف البيانات مع إبقائه متاحاً للـcron.
6. مخرجات البوابات الثلاث **حرفياً**.
7. أي اختبار من السبعة لم تنفّذه ولماذا.
8. **ما تبقّى من فرص تحسين لم تنفّذها** — للمالك ليقرّر لاحقاً.

---

## 🛑 عند الانتهاء

**توقّف.** اكتب: **«المراحل الأربع اكتملت.»**

**إن اكتشفت أن جزءاً من الخطة خاطئ — توقّف وأبلغ بدل الالتفاف حوله.**
