# مهمة: توحيد مصدر فئات المصاريف

> إصلاح واجهة وبيانات. **لا منطق محاسبي، لا migration، لا مساس بالربح.**

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً** — §3 (حمايات مثبَّتة)، §10 (RTL والنقود)، §11 (بوابات الفحص).

المستخدم **شخص واحد** — مالك ورشة، ليس تقنياً، **على الهاتف 95% من الوقت** (360–390px).

### شكوى المالك

> «في فئات جاهزة للاختيار منها عند إضافة مصروف — هل يمكن التحكم بها وكيف ومن أين؟ وهل هي ضمن أفضل الممارسات؟»

---

## 1. التشخيص المؤكَّد (مفحوص على الكود وعلى قاعدة الإنتاج)

### م-1 — مصدران متضاربان للفئات 🔴

| الموضع | المصدر |
|---|---|
| **الفلتر** في رقاقة «مصاريف» | ثابت `EXPENSE_CATEGORIES` **مكتوب في الكود** (`PaymentsTab.tsx:32-42`) |
| **الإدخال** في النموذج | `useExpenseCategoryCatalog()` من **قاعدة البيانات** (`SmartFinanceForm.tsx:243`) |

**الأثر:** الفلتر يعرض فئات لا يستعملها المالك («إيجار»، «كهرباء ومياه»)، وفئاته الحقيقية **لا تظهر في الفلتر إطلاقاً** فلا يستطيع تصفيتها.

### م-2 — كتالوج الفئات فارغ

فُحص على قاعدة الإنتاج: `expense_category_catalog` فيه **صفر صفوف**. فقائمة الاختيار في النموذج فارغة، والمالك يكتب يدوياً في كل مرّة.

### م-3 — الكتابة الحرّة أنتجت تكراراً فعلياً

الفئات المستعملة في `expense` على الإنتاج اليوم:

```
حوافز  ·  راتب المالك  ·  راتب مالك  ·  راتب مالك (بمسافة زائدة)  ·  رواتب  ·  سحب تطبيق البنك
```

**أربع منها تعني الشيء نفسه** بأربع كتابات. التصفية بإحداها لا تُظهر البقيّة.

### ما هو سليم ولا يُلمس

البنية التحتية **كاملة وتعمل**: جدول `expense_category_catalog`، والدوال `getExpenseCategoryCatalog` / `createExpenseCategoryCatalog` / `updateExpenseCategoryCatalog` / `deleteExpenseCategoryCatalog` (`finance/actions.ts` ~1823+)، ومودال `FinanceCatalogModal`. **أعِد استخدامها — لا تُعِد بناءها.**

---

## 2. الإصلاح 1 — مصدر واحد للفئات

**احذف** الثابت `EXPENSE_CATEGORIES` من `PaymentsTab.tsx:32-42`، واجعل الفلتر يقرأ من **نفس مصدر النموذج** (`useExpenseCategoryCatalog`).

**متطلّبات:**
- الخيار الأول يبقى «كل الفئات» (قيمة `all`) — سلوك قائم لا يتغيّر.
- **معالجة الفئات اليتيمة:** فئة مستعملة في `expense` لكنها ليست في الكتالوج **يجب أن تبقى قابلة للتصفية**. ادمج فئات الكتالوج مع الفئات المستعملة فعلياً (`distinct category from expense`) في قائمة واحدة بلا تكرار.
  ⚠️ **لا تُسقط أي فئة يستعملها المالك اليوم** — وإلا فقد القدرة على تصفية بياناته القائمة.
- إن لزم استعلام جديد لجلب الفئات المستعملة، أضفه في `finance/queries.ts` على نمط الدوال القائمة.
- ⚠️ **معامل `?category=` يجب أن يبقى يعمل** كما هو (توافق عكسي).

---

## 3. الإصلاح 2 — بذر الكتالوج

الكتالوج فارغ. ابذره بفئات **افتراضية معقولة لورشة نباتات وزراعة**، مع الفئات المستعملة فعلياً.

**الطريقة الإلزامية:** بذر **عند القراءة** (lazy) أو عبر إجراء يستدعيه المالك — **لا migration ولا سكربت يُشغَّل على الإنتاج**.

اقتراح للفئات الافتراضية (عدّله إن رأيت أفضل منه):
`رواتب · إيجار · كهرباء ومياه · نقل وتوصيل · تعبئة وتغليف · صيانة وأدوات · حوافز · أخرى`

⚠️ **البذر يجب أن يكون:**
- **آمناً للتكرار** — لا يُنشئ نسخاً مكرّرة إن نُفِّذ مرّتين.
- **غير مدمِّر** — لا يحذف ولا يُعدّل أي فئة قائمة.
- **لا يمسّ صفوف `expense`** إطلاقاً — البذر يخصّ الكتالوج وحده.

---

## 4. الإصلاح 3 — الكتابة الحرّة تُغذّي الكتالوج

اليوم: يكتب المالك فئة جديدة يدوياً فتُخزَّن في `expense.category` **بلا أن تدخل الكتالوج** — فلا تظهر له لاحقاً ويكتبها من جديد بصياغة مختلفة (وهذا سبب م-3).

**المطلوب:** عند حفظ مصروف بفئة مكتوبة يدوياً غير موجودة في الكتالوج، تُضاف إليه تلقائياً.

**متطلّبات:**
- **طبّع النصّ قبل المقارنة والحفظ:** `trim()` على الأقل. «راتب مالك» و«راتب مالك » (بمسافة) يجب ألّا تُنشئا صفّين.
- المقارنة **غير حسّاسة للمسافات الطرفية**. قرار حساسية حالة الأحرف لك — صرّح به.
- **لا تُفشل حفظ المصروف** إن فشلت إضافة الفئة — المصروف هو الأهم. سجّل الخطأ وامضِ.
- احترم قيد `<= 200` حرفاً على اسم الفئة.

⚠️ **لا تُنشئ فئة من نصّ فارغ.**

---

## 5. ما لا يدخل هذه المهمة

**لا تُوحّد الفئات المكرّرة القائمة** (`راتب مالك` / `راتب المالك` / `رواتب`) في صفوف `expense`.

السبب: المالك لم يقرّر بعد كيف يعالج تلك الصفوف السبعة (قرار منفصل قيد النقاش عنده). أي تعديل عليها الآن يُفسد قراره.

**اقتصر على الكتالوج والواجهة.** إن رأيت الحاجة لتوحيد رجعي، **اذكره في تقريرك كتوصية** ولا تُنفّذه.

---

## 🔒 خطوط حمراء

1. **لا تلمس `pnl.ts`** ولا أي منطق محاسبي. لا علاقة لهذه المهمة بالربح.
2. **لا migration ولا تعديل مخطط.** `drizzle-kit generate` يجب أن يطبع «No schema changes, nothing to migrate 😴».
3. **لا تُشغّل شيئاً على قاعدة الإنتاج** — لا سكربت، لا SQL، لا بذر يدوي.
4. **لا تُعدّل ولا تحذف أي صفّ `expense` قائم** (§5).
5. **لا تلمس** `getPayments` ولا استعلامات `queries.ts` القائمة إلا بإضافة ما يلزم للفئات — أُصلحت مؤخّراً بعد أعطال وقت تشغيل.
6. **لا تكسر التوافق العكسي:** `?category=` · `?filter=` · `?tab=` · `?source=` · `?nature=`.
7. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`text-start`/`text-end`. ممنوع `pl-`/`pr-`/`left-`/`right-`.
8. **أهداف اللمس ≥ 44px**، 360px و390px، **لا تمرير أفقي**.
9. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ.
10. **لا تُعِد بناء** `FinanceCatalogModal` ولا دوال الكتالوج القائمة — أعِد استخدامها.

---

## بوابات الفحص — قبل كل كومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build                # البوابة الحقيقية
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **`typecheck` وحده لا يكفي** — لا يكشف خرق حدود الخادم/العميل.

⚠️ **وحتى `pnpm build` لا يكشف أخطاء وقت التشغيل.** جولة سابقة اجتازت البوابات الثلاث وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً** (خطأ SQL). إن كتبت استعلاماً جديداً، **لا تفترض صحّته من البناء**.

---

## الاختبار المطلوب

نفّذ ما تستطيع. **قل صراحةً ما لم تستطع ولماذا — ولا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.**

1. **فلتر الفئات يعرض نفس فئات النموذج** — لا فئات وهمية ولا فئات ناقصة.
2. **الفئات المستعملة اليوم كلها قابلة للتصفية** — بما فيها «حوافز» و«سحب تطبيق البنك» و«راتب مالك» بصياغاتها.
3. الكتالوج مبذور، والبذر **لا يُكرّر** عند إعادة التنفيذ.
4. فئة جديدة مكتوبة يدوياً → تظهر في الكتالوج وفي الفلتر بعدها.
5. **«راتب مالك» و«راتب مالك » (بمسافة) لا تُنشئان صفّين** في الكتالوج.
6. زرّ «إدارة الفئات» ما زال يعمل (إضافة/تعديل/حذف).
7. `?category=رواتب` ما زال يعمل.
8. 360px: لا تمرير أفقي، أهداف ≥44px.
9. `/reports` → «فحص الآن» → **الكل PASS أو WARN، ولا واحد FAIL**.
   ℹ️ **ملاحظة:** `IC-12 = WARN` موجود مسبقاً (أرصدة مخزون سالبة) — **ليس من عملك**، تجاهله.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.** المالك يراجع محلياً.
- كومت منفصل لكل إصلاح (1 / 2 / 3).
- رسائل بالعربية تشرح **لماذا** لا ماذا فقط.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. ما نُفِّذ وما لم يُنفَّذ **ولماذا**.
2. **مخرجات البوابات الثلاث حرفياً.**
3. **كيف عالجت الفئات اليتيمة** — أثبت أن كل فئة مستعملة اليوم ما زالت قابلة للتصفية.
4. **آلية البذر** التي اخترتها ولماذا، وكيف ضمنت أنها آمنة للتكرار.
5. قرارك في حساسية حالة الأحرف عند مطابقة الفئات.
6. أي اختبار من التسعة **لم** تنفّذه ولماذا.
7. **توصيتك بشأن توحيد الفئات المكرّرة رجعياً** — كتوصية فقط، لا تنفيذ (§5).

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود الفعلي، **وستُختبر الفئات على قاعدة البيانات قبل أي رفع**.

**إن اكتشفت أن جزءاً من التشخيص خاطئ — توقّف وأبلغ بدل الالتفاف حوله.**
