# استشارة — لا تنفيذ: نظام الأزرار وثبات الواجهة في Zman

> ## 🛑 مهمة رأي فقط
>
> **لا تكتب كوداً. لا تُعدّل ملفاً. لا تُنشئ كومت.**
>
> اقرأ الكود بحرّية (Read / grep / بناء الفهم). المطلوب **تقرير رأي مكتوب** يُعرَض على المالك ليقرّر.
>
> استشارة سابقة قُدِّمت بناءً على **صور فقط** بلا اطّلاع على الكود — فأخطأت في وقائع أساسية (§5). **أنت تملك الكود: احكم منه، لا من الانطباع البصري.**

---

## 1. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً** — §3 (حمايات مثبَّتة)، §10 (RTL، والمستخدم على الهاتف 95%).

المستخدم **شخص واحد** — مالك ورشة صغيرة، ليس تقنياً، **على الهاتف 95% من الوقت** (360–390px). حجم البيانات صغير (عشرات السجلات لا آلاف) — أي حلّ بحجم تطبيق مؤسّسي مرفوض.

### شكوى المالك

> «الأزرار التي تظهر في المالية والانتقالات بين التبويبات — التبويبان الرئيسيان يظهر ويختفي معهم أزرار أخرى.»

---

## 2. الجذر المرشَّح — تحقّق منه أولاً

`src/components/shared/PageToolbar.tsx` هو شريط الأدوات الموحَّد. تعليقه التوثيقي (~السطر 48-51) يقول حرفياً:

> «كل صفحة تمرّر ما يلزمها فقط… **الأزرار الغائبة لا تترك أثراً (لا حجز مساحة)**»

**هذا قرار تصميمي مقصود** — وهو المرشَّح الأول لتفسير الشكوى: الشريط يتقلّص ويتمدّد حسب ما تمرّره كل شاشة/تبويب، فتتحرّك مواضع الأزرار الباقية.

عقد المكوّن: `search` · `filters` · `menuItems` · `filterSlot` · `leading` · `trailing` — كلها اختيارية.

**قيّم هذا القرار:** هل «لا حجز مساحة» صحيح لواجهة تبديل الأوضاع فيها متكرّر؟ ما البدائل (حجز مكان ثابت، أو زر معطَّل، أو مجموعة ثابتة بمحتوى متغيّر)، وما كلفة كلٍّ منها على 360px؟

---

## 3. النطاق — سبع شاشات تشترك في نفس الشريط

`PageToolbar` مستعمل في:

- `src/app/(app)/finance/FinanceClient.tsx`
- `src/app/(app)/orders/OrdersClient.tsx`
- `src/app/(app)/catalog/CatalogClient.tsx`
- `src/app/(app)/snippets/SnippetsClient.tsx`
- `src/features/orders/components/OrderList.tsx`

ملاحظة أوّلية: الاستعمال **متباين** — بعضها يمرّر `leading`، بعضها `actions`/`trailing`، بعضها `search` فقط. تحقّق من التباين وقيّمه.

**اشمل أيضاً** الشاشات التي لا تستعمل `PageToolbar` (`/assets`, `/inventory`, `/reports`, `/activities`, لوحة التحكم) — هل تبني أشرطتها الخاصة؟ هذا تشتّت في نظام التصميم يستحق الرصد.

**المطلوب: جدول مقارنة** — لكل شاشة: ما تمرّره، ما يظهر فعلياً، وأين تختلف عن أخواتها بلا مبرّر.

---

## 4. 🔴 قيد صارم على أي حلّ تقترحه

> **لا تنقل أي وظيفة موجودة اليوم داخل التبويبات إلى خارجها.**

ممنوع: نقل زرّ/فلتر/إجراء إلى قائمة «المزيد»، أو شريط جانبي، أو صفحة إعدادات مستقلّة، أو خلف قائمة ثانوية تُخرجه من سياق التبويب.

**السبب:** المالك يريد أدواته في مكان عمله. الحلّ يجب أن يُنظّم ما هو موجود **داخل** الشاشة — لا أن يُفرّغها بالنقل.

مسموح: إعادة الترتيب داخل الشاشة، الدمج، تغيير الشكل (رقاقة → منسدلة)، حجز مساحة ثابتة، تغيير التسميات، تجميع عناصر في مكوّن واحد داخل نفس التبويب.

---

## 5. ⚠️ وقائع مؤكَّدة من الكود — لا تُكرّر أخطاء الاستشارة السابقة

استشارة سابقة بُنيت على صور فقط. هذه أخطاؤها، مصحَّحة بالكود:

| ادّعاء خاطئ | الحقيقة |
|---|---|
| «رأس مال / ثابتة / متغيّرة» مستوى ثالث من أزرار التصفية | **شارات وصفية داخل البطاقات** (`PaymentsTab.tsx:292-298`) — ليست أزراراً. المستويات **اثنان** لا ثلاثة |
| الزرّ العائم يغطّي البطاقات | يوجد `pb-36` (144px) حماية صريحة (`PaymentsTab.tsx:193`) |
| فلاتر خاصة بالمشتريات تظهر كشرائح | لا وجود لها |

**تحقّق من كل واقعة تبني عليها.** إن ناقض الكود ما هنا، فالكود هو الحكم.

### ما تأكّد فعلاً (تحقّق منه أنت أيضاً)

1. **تمرير أفقي في صفّ الرقاقات** — `PaymentsTab.tsx:195` فيه `overflow-x-auto`. عند فلتر «مصاريف» يحمل الصفّ: 4 رقاقات + منسدلة فئات + زرّ «إدارة الفئات». **يخالف قاعدة `CLAUDE.md`: لا تمرير أفقي على الهاتف.**
2. **الظهور المشروط انتقل ولم يُحلّ** — جولة سابقة نقلت فلتر الفئة من الهيدر إلى صفّ الرقاقات. الهيدر ثبت، **وصار صفّ الرقاقات هو ما يتقلّص ويتمدّد**.
3. **زرّ الفلتر في «مبيعاتي» فقط** — `FinanceClient.tsx:173`: `activeTab === "sales" ? [...] : null`.
4. **أهداف لمس دون المعيار** — الرقاقات `min-h-[40px]`، المنسدلة والزرّ `min-h-[36px]`. القاعدة **≥ 44px**.

---

## 6. المطلوب منك

### أ. تشخيص جذري (لا عرَضي)

لا تكتفِ بـ«هذا الزرّ يظهر ويختفي». اذهب للسبب البنيوي: هل المشكلة في عقد `PageToolbar`؟ في تفويض القرار لكل شاشة بلا قاعدة؟ في خلط مستويات (تنقّل / تصفية / إجراء) في شريط واحد؟ في غياب تعريف لما يجب أن يبقى ثابتاً؟

**اذكر لكل مشكلة: الجذر، أين يظهر (ملف:سطر)، وما يشعر به المالك.**

### ب. خريطة الظهور والاختفاء

جدول لكل عنصر تحكّم في المالية وبقيّة الشاشات: أين يظهر، متى يختفي، ما الذي يتحرّك بسببه، وهل الاختفاء مبرّر أم اعتباطي.

### ج. حلّ ملموس ضمن القيد

مبادئ عامّة لا تكفي. اقترح **بنية محدَّدة** — أي العناصر تبقى ثابتة دائماً، وأيّها يتغيّر محتواه دون موضعه، وكيف يتّسع ذلك على 360px بلا تمرير أفقي وبأهداف ≥44px.

ارسم التخطيط المقترح نصّياً لكل حالة (مدفوعاتي/الكل · مدفوعاتي/مصاريف · مبيعاتي · الحسابات).

### د. قاعدة قابلة للتطبيق على كل الشاشات

الشكوى في المالية، لكن `PageToolbar` مشترك. اقترح **قاعدة واحدة** تُطبَّق على السبع، وبيّن أثرها على كل شاشة — بما فيها ما قد ينكسر.

### هـ. ترتيب بالأولوية والمخاطرة

ما يُنفَّذ أولاً، ما يُؤجَّل، وما لا يستحق. لكل بند: الأثر على المالك، وحجم الشغل، والخطر.

---

## 7. أسئلة أريد رأيك فيها صراحةً

1. هل «لا حجز مساحة» في `PageToolbar` قرار صحيح أم هو الجذر؟ وما البديل الأنسب لواجهة أوضاع متبدّلة؟
2. صفّ الرقاقات في «مدفوعاتي» يزدحم عند «مصاريف». **ضمن القيد (لا نقل خارج التبويب)** — كيف تحلّه؟
3. زرّ الفلتر: يُوحَّد بإظهاره دائماً، أم يُلغى ويُدمج في الرقاقات، أم يبقى مشروطاً؟
4. التبويبان يظهران كأيقونتين بلا نصّ (`FinanceClient.tsx` — `HeaderIconButton variant="tab"`). هل هذا مناسب لمستخدم غير تقني على 360px؟
5. **ملخّص النتائج** (عدد + إجمالي فوق القائمة) اقتُرح سابقاً. ⚠️ تحذير: النظام مبنيّ على «رقم ربح واحد يثق به المالك»، ومجموع بصري فوق قائمة مُصفّاة قد **يخالف** رقم `pnl.ts` (الأصول والهدر يُعالجان مختلفاً). هل يستحق المخاطرة؟ وإن نعم، فبأي صياغة لا تُنتج رقماً منافساً؟
6. ما الذي فات كل التحليلات السابقة؟
7. بديل مختلف كلّياً إن رأيته — مرحَّب به، وهو أهمّ ما قد تُقدّمه.

---

## 8. قيود على أي حلّ

1. **لا نقل وظيفة خارج التبويبات** (§4) — الأهم.
2. **لا مساس بالمنطق المحاسبي** — `pnl.ts` وLOCKED-6. الحلّ في طبقة العرض.
3. **لا migration ولا تغيير مخطط.**
4. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`. ممنوع `pl-`/`pr-`/`left-`/`right-`.
5. **الهاتف أولاً:** 360px و390px، أهداف ≥44px، **لا تمرير أفقي**.
6. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ.
7. **حجم يناسب مستخدماً واحداً بعشرات السجلات** — لا لوحات فلاتر بستّة حقول ولا حفظ حالة معقّد ما لم تُبرّره بوضوح.

---

## 9. شكل التقرير

بالعربية، مباشر بلا مجاملة:

1. **التشخيص الجذري** — كل مشكلة بجذرها ودليلها (ملف:سطر) وأثرها على المالك.
2. **خريطة الظهور/الاختفاء** — الجدول المطلوب في §6.ب.
3. **حكمك على الوقائع الأربع المؤكَّدة** في §5 — أكّدها أو انقضها بدليل.
4. **البنية المقترحة** مع تخطيط نصّي لكل حالة.
5. **القاعدة العامّة** لكل الشاشات وأثرها على كلٍّ منها.
6. **الترتيب بالأولوية والمخاطرة.**
7. **إجابات مباشرة** على الأسئلة السبعة في §7.
8. **ما لم تستطع التحقّق منه ولماذا** — لا تدّعِ فحصاً لم تقم به.

**كن صريحاً.** إن كان التشخيص هنا خاطئاً أو مبالغاً فيه، قل ذلك بدليل — قيمة الاستشارة في استقلال رأيك. وإن رأيت القيد في §4 يمنع الحلّ الأمثل، **قله صراحةً واشرح الكلفة** — لكن قدّم أفضل حلّ ممكن ضمنه أيضاً.

**لا تُعدّل أي ملف. التقرير فقط.**
