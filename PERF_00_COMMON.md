# مهمة: إصلاح أداء Zman — من ~11 ثانية إلى ~2

> ⚠️ **مهمة أداء بحتة. الأرقام المالية يجب ألّا تتغيّر بفلس واحد.**
>
> أربع موجات مرتّبة بالمخاطرة. **نفّذها بالترتيب** — كل موجة كومت مستقلّ.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً — كلّه**، خصوصاً §2 (النموذج المالي وLOCKED-6)، §3 (حمايات مثبَّتة)، §10، §11.

المستخدم **شخص واحد** — مالك ورشة، **على الهاتف 95% من الوقت**.

### الشكوى

> «النظام بطيء، وخاصة عند فتحه. وأحياناً يعطي: حدث خطأ أثناء تحميل البيانات.»

---

## 1. التشخيص — مقيس فعلياً على قاعدة الإنتاج، لا مقدَّر

| القياس | القيمة |
|---|---|
| زمن الشبكة لكل استعلام (RTT) | **~150 ms** |
| الاتصال البارد الأول | **~1,320 ms** |
| `computeOperatingPnl` (استدعاء واحد) | **~1,560 ms** |
| `getMonthlyProfit` (6 أشهر) | **~10,755 ms** |
| نفس الستّة بالتوازي | **~3,211 ms** |

**حجم البيانات: 83 حركة صندوق · 16 مصروفاً · 37 شراءً · 27 مبيعة.**

### 🔴 الاستنتاج الحاسم — افهمه قبل أن تكتب سطراً

قاعدة البيانات **لا تحسب شيئاً**. `select 1` التافه يستغرق 150ms، واستعلام حقيقي 135ms. الخادم يعالج في أقلّ من مللي ثانية؛ **الباقي كلّه شبكة**.

> **المشكلة ليست استعلامات بطيئة — بل عدد الرحلات.**
> 36 رحلة × 150ms = 5.4 ثانية انتظار صافٍ.

**كل قرار في هذه المهمة يُقاس بمعيار واحد: هل يقلّل عدد الرحلات؟**

**ولذلك:** لا تُضِف فهارس، ولا تُحسّن استعلامات، ولا تُخزّن نتائج محسوبة. بـ83 صفاً هذا كلّه بلا فائدة، والأخير يخلق مصدر حقيقة ثانياً يخالف LOCKED-6.

---

## 🔒 الخطّ الأحمر الأول — الأرقام المالية

`pnl.ts` محروس بـ**LOCKED-6**: تعريف الربح يجب أن يتطابق في ثلاثة مواضع (لوحة التحكم · التقارير · الرسم الشهري)، و`IC-13` يفحصه وقت التشغيل.

**إلزامي على أي تغيير يمسّ `pnl.ts` أو `dashboard/queries.ts`:**

1. **سجّل الأرقام قبل التغيير** (الربح، النقد، إجمالي الأصول، `equityDriftCents`).
2. نفّذ التغيير.
3. **سجّلها بعده وأثبت التطابق بالفلس.**
4. شغّل `/reports` → «فحص الآن» → **صفر FAIL**.

**إن اختلف رقم واحد — تراجع فوراً وأبلغ. لا تُكوّت.**

ℹ️ `IC-12 = WARN` (أرصدة مخزون سالبة) **موجود مسبقاً** — ليس من عملك، تجاهله.

---

## 🌊 الموجة 1 — توازي الاستعلامات (خطر منخفض، أثر كبير)

### 1.أ — `computeOperatingPnl` (`finance/pnl.ts`)

ستّة استعلامات **متسلسلة** (الأسطر ~141، 162، 204، 257، 305، 339) وصفر `Promise.all`. كلها **قراءة مستقلّة** — لا يعتمد أيّها على نتيجة الآخر.

**المطلوب:** اجمعها في `Promise.all` واحد.

⚠️ **تحقّق بنفسك من الاستقلال قبل التوازي.** إن وجدت استعلاماً يعتمد على سابقه، **اتركه متسلسلاً وقل ذلك** — لا تُخاطر بالأرقام لأجل السرعة.
⚠️ `getDepreciationForPeriodCents` يُستدعى ضمنها — تأكّد أنه آمن للتوازي.

**المتوقّع:** ~1,560ms → ~250ms.

### 1.ب — `getMonthlyProfit` (`dashboard/queries.ts` ~679)

حلقة `for` تنتظر كل شهر قبل التالي. الأشهر مستقلّة تماماً.

**المطلوب:** `Promise.all` على الأشهر الستّة، **مع الحفاظ على ترتيبها في النتيجة**.

**المتوقّع:** ~10,755ms → ~1,500ms (وأقلّ بعد 1.أ).

### 1.ج — `finance/queries.ts`

فيه 15 `await` متتالياً وصفر `Promise.all`. **افحص كل دالة**: ما كان مستقلّاً وازِه، وما كان متعاقباً اتركه.

---

## 🌊 الموجة 2 — إصلاحات الاتصال والكاش (خطر منعدم)

### 2.أ — `lib/db/client.ts`

```ts
connect_timeout: 10,   // ← ارفعه إلى 30
// @ts-expect-error postgres-js types may not include retry in Options
retry: 1,              // ← احذفه: هذا الخيار لا وجود له في postgres.js
```

`retry` **غير موجود في المكتبة إطلاقاً** — سطر لا يفعل شيئاً، وتعليقه يوحي بحماية وهمية. احذف السطرين معاً.

🔴 **لا تُفعّل `prepare: true`** — الاتصال عبر Supavisor pooler (منفذ 6543) و**لا يدعم Prepared Statements**. تفعيله يكسر الاتصال.

### 2.ب — `providers/query-provider.tsx`

`staleTime: 30 * 1000` → **5 دقائق**.

مع `refetchOnWindowFocus: true`، كل عودة للتطبيق تُطلق موجة كاملة. `gcTime` (24 ساعة) والكاش الدائم يبقيان كما هما.

### 2.ج — لا شاشة خطأ ما دام هناك كاش 🔴

اليوم `DashboardClient` يعرض `ErrorState` عند أول فشل — **حتى لو كانت البيانات محفوظة في `localStorage`**.

**المطلوب:** إن وُجد كاش، اعرضه دائماً مع **شريط تنبيه مرئي**: «تعذّر التحديث — البيانات قد تكون قديمة».

⚠️ **الشريط إلزامي، وليس تجميلياً.** عرض رقم مالي قديم بلا تنبيه يخالف مبدأ النظام («رقم واحد يثق به المالك»). بلا كاش، تبقى `ErrorState` كما هي.

---

## 🌊 الموجة 3 — نقطة التجميع (الأكبر أثراً)

**هذه أهمّ موجة.** لوحة التحكم تُطلق **7 استدعاءات منفصلة** من المتصفّح، كلٌّ برحلة شبكة مستقلّة (~150ms + وقت المعالجة).

**المطلوب:** دالة **واحدة** `getDashboardBundle(params)` في `dashboard/queries.ts` تُنفّذ كل استعلامات لوحة التحكم في `Promise.all` **داخل الخادم** وتُعيدها دفعة واحدة.

```
اليوم:  المتصفّح → 7 رحلات مستقلّة
بعده:   المتصفّح → رحلة واحدة → كل شيء بالتوازي داخل الخادم (~0ms بينها)
```

**متطلّبات:**
- هوك واحد `useDashboardBundle` يحلّ محلّ السبعة في `DashboardClient`.
- **لا تحذف الدوال الأصلية** — قد تُستعمل في شاشات أخرى. تحقّق بـgrep قبل أي حذف.
- **حالات التحميل والخطأ تبقى تعمل** — حزمة واحدة تعني حالة واحدة؛ تأكّد أن الواجهة لا تنكسر.
- **لا تُغيّر أي حساب** — الحزمة تُجمّع فقط، لا تُعيد تعريف شيء.
- `getFinancialSummary` يُستدعى **مرّتين** في `DashboardClient` (الفترة + كل الفترات) — أدرجهما في الحزمة بلا تكرار العمل.

⚠️ **اقتصر على لوحة التحكم في هذه الموجة.** الكتالوج (21 استدعاءً) والطلبات (15) في الموجة 4.

---

## 🌊 الموجة 4 — الشاشات المهمَلة وN+1

### 4.أ — `useComponentStock` — نمط N+1 مؤكَّد 🔴

`CatalogClient.tsx:362` داخل بطاقة صنف: **استعلام مستقلّ لكل صنف متتبَّع**. عشرون صنفاً = عشرون رحلة.

**المطلوب:** استعلام واحد يجلب أرصدة **كل** الأصناف دفعة واحدة، والبطاقات تقرأ منه.

موثَّق في `CLAUDE.md` كـ**م-9**. يُستعمل أيضاً في محرّر المكوّنات ونموذج الطلب — **تحقّق من كل المستدعين** قبل التغيير.

### 4.ب — الكتالوج والطلبات

`CatalogClient` فيه ~21 استدعاء هوك و`OrdersClient` ~15. **قِس أولاً**، ثم طبّق نفس نمط الحزمة إن ثبتت الفائدة.

**لا تُعمّم بلا قياس.** إن وجدت أن استدعاءات كثيرة شرطية أو خفيفة، قل ذلك ولا تُغيّر.

### 4.ج — إحماء يمنع سبات Supabase

**غير موجود اليوم.** الاتصال البارد يكلّف ~1,320ms، وهو سبب «بطء أول فتحة».

**المطلوب:** مسار `GET /api/health` خفيف يُنفّذ `select 1` فقط، مع **Vercel Cron** يستدعيه كل بضع دقائق.

⚠️ **لا يوجد `vercel.json` في المشروع** — Vercel مُعدّ عبر Dashboard (§10 في `CLAUDE.md`). إنشاء `vercel.json` لأجل الـcron **قد يُبطل إعدادات Dashboard ويكسر النشر**.

🔴 **لذلك: أنشئ المسار فقط، ولا تُنشئ `vercel.json`.** اكتب في تقريرك تعليمات إعداد الـcron يدوياً من Dashboard، واترك القرار للمالك. **إن رأيت `vercel.json` ضرورياً — توقّف وأبلغ، ولا تُنشئه.**

المسار **لا يقرأ بيانات ولا يُرجع أرقاماً** — `select 1` فقط.

---

## 🔒 خطوط حمراء

1. **الأرقام المالية لا تتغيّر بفلس.** أثبت ذلك قبل/بعد (§الخطّ الأحمر الأول).
2. **لا تُغيّر تعريف الربح** (LOCKED-6). التوازي لا يُغيّر منطقاً — إن اضطررت لتغيير منطق، توقّف وأبلغ.
3. **لا migration ولا تعديل مخطط.** `drizzle-kit generate` يبقى يطبع «No schema changes».
4. **لا تُشغّل شيئاً على قاعدة الإنتاج** — لا سكربت ولا SQL.
5. **لا فهارس جديدة ولا نتائج محسوبة مخزَّنة** — بـ83 صفاً بلا فائدة، والثاني يخالف LOCKED-6.
6. **لا `prepare: true`** — يكسر Supavisor pooler.
7. **لا `vercel.json`** (§4.ج).
8. **لا تلمس** `getPayments` ولا `getReceivables` — أُصلحت مؤخّراً بعد أعطال وقت تشغيل.
9. **لا تكسر التوافق العكسي:** `?tab=` · `?filter=` · `?category=` · `?source=` · `?nature=`.
10. **RTL — خصائص منطقية فقط.** أهداف اللمس ≥44px. لا تمرير أفقي.
11. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ.
12. **لا تحذف دالة** قبل التحقّق بـgrep أنها بلا مستدعٍ.

---

## بوابات الفحص — قبل كل كومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **البوابات الثلاث لا تكشف أخطاء وقت التشغيل.** جولة سابقة اجتازتها كلها وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً** (خطأ SQL في `UNION ALL`). **هذه مهمة أداء — لا تفترض صحّة شيء من البناء.**

---

## القياس المطلوب — إلزامي

**هذه مهمة أداء: بلا أرقام قبل/بعد لا قيمة لعملك.**

قِس بنفسك (شغّل الدوال فعلياً، لا تُقدّر):

| # | القياس | قبل (مقيس) |
|---|---|---|
| 1 | `computeOperatingPnl` استدعاء واحد | ~1,560 ms |
| 2 | `getMonthlyProfit` | ~10,755 ms |
| 3 | حزمة لوحة التحكم مقابل السبعة المنفصلة | — |

⚠️ **أحمِ قبل القياس** — أول استدعاء يحمل كلفة الاتصال البارد (~1,320ms) ويُفسد النتيجة.

---

## الاختبار المطلوب

**قل صراحةً ما لم تنفّذه ولماذا. لا تعلّم ✅ على اختبار استنتجته من الكود.**

1. **الأرقام المالية متطابقة بالفلس** قبل/بعد — الربح، النقد، الأصول، `equityDrift`.
2. `/reports` → «فحص الآن» → **صفر FAIL** (`IC-12 = WARN` مقبول ومسبق).
3. لوحة التحكم تعرض نفس الأرقام تماماً.
4. حالات التحميل والخطأ تعمل بعد الحزمة.
5. **الكاش القديم يُعرض مع شريط التنبيه** بدل شاشة الخطأ.
6. الكتالوج يعرض أرصدة صحيحة بعد إصلاح N+1.
7. `/api/health` يُرجع 200 بلا بيانات.
8. كل معاملات §9 تعمل.
9. 360px: لا تمرير أفقي، أهداف ≥44px.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.**
- **كومت منفصل لكل موجة** (1/2/3/4).
- رسائل بالعربية تشرح **لماذا**، مع **أرقام القياس**.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. **جدول قبل/بعد بالأرقام المقيسة** — أهمّ مخرَج.
2. **إثبات تطابق الأرقام المالية بالفلس.**
3. مخرجات البوابات الثلاث **حرفياً**.
4. ما نُفِّذ وما لم يُنفَّذ **ولماذا**.
5. أي استعلام تركته متسلسلاً ولماذا (§1.أ).
6. نتيجة قياس الكتالوج والطلبات، وهل عمّمت الحزمة (§4.ب).
7. **تعليمات إعداد Vercel Cron يدوياً** للمالك.
8. أي اختبار من التسعة لم تنفّذه ولماذا.

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود، **وستُقاس الأرقام على قاعدة بيانات فعلية قبل أي رفع**.

**إن اكتشفت أن جزءاً من الخطة خاطئ أو يُهدّد الأرقام — توقّف وأبلغ فوراً بدل الالتفاف حوله.**
