# مهمة: إعادة هيكلة شاشة المالية — «مدفوعاتي» الموحَّد + إصلاح الأصول المفقودة

> اقرأ الوثيقة كاملة قبل أي كود. المراحل مرتّبة بالمخاطرة: المرحلة 1 تُصلح فجوة بيانات قائمة، والمرحلة 3 هي الأكبر أثراً والأعلى خطراً.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في جذر المستودع أولاً** — §2 (النموذج المالي)، §3 (حمايات مثبَّتة)، §10 (RTL والنقود)، §11 (بوابات الفحص).

المستخدم **شخص واحد** — مالك ورشة، ليس محاسباً، **على الهاتف 95% من الوقت** (360–390px).

هذه المهمة نتيجة **استشارتين مستقلّتين** اتفقتا على التشخيص. شكوى المالك الأصلية:

> «ضايع في المالي، شو الفرق بين المصاريف والمشتريات؟ عامل مشكلة وتكرار بالكونسبت. بدي واجهة واحدة.»
> «إذا ما وحّدت، التكرار رايح يضل موجود وكل مرة المستخدم يحتار وين وكيف يدخل.»

### الشكل النهائي المطلوب

```
┌──────────────────────────────────────┐
│   [ مدفوعاتي ]      [ مبيعاتي ]      │  ← تبويبان بدل ثلاثة
│   🔍 بحث...                          │
│   الكل · مصاريف · مشتريات · أصول    │  ← فلتر رقاقات
├──────────────────────────────────────┤
│  ☕ قهوة للورشة          12.500 د.أ  │
│  [أخرى]                منذ 3 أيام   │
├──────────────────────────────────────┤
│  🛒 سماد عضوي            45.000 د.أ  │
│  المورد: أحمد · 10×4.500  منذ 5 أيام│
├──────────────────────────────────────┤
│  🔧 مقصّ تقليم            80.000 د.أ  │
│  [رأس مال]              منذ أسبوع   │
├──────────────────────────────────────┤
│  📦 تلف شتلات            15.000 د.أ  │  ← رمادي
│  خسارة مخزون — بلا دفع   منذ أسبوعين│
└──────────────────────────────────────┘
              ⊕ تسجيل جديد              ← نصّ ثابت
```

---

## المرحلة 1 (الأعلى خطراً — ابدأ بها) — أصول مفقودة من `/assets`

### الخلل المؤكَّد

`AssetsScreen` تستعمل `useCapitalAssets()` → `getAllCapitalAssets()` في `src/features/depreciation/assetsQueries.ts` — تقرأ **جدول `capital_asset` فقط**.

وجدول `capital_asset` يُملأ **فقط** عبر `addCapitalAsset()`. تحقّق من `SmartFinanceForm.tsx:686-691`:

```ts
const handleConfirmDeductOnce = () => {
  toast.info("تم تسجيل الأصل كإضافة رأسمالية — لا إهلاك شهري");
  setPendingAsset(null);   // ← لا استدعاء لـ addCapitalAsset إطلاقاً
  ...
};
```

**النتيجة:** المالك يسجّل أصلاً ويختار «خصم مرة واحدة» → يُنشأ صف `expense`/`purchase` بـ`isCapitalAsset=true`، ولا يُنشأ صف في `capital_asset` → **الأصل لا يظهر في `/assets` إطلاقاً**. يبقى مدفوناً بين المصاريف. لا مسار لجرده.

### المطلوب

في `/assets`، اعرض **قسمين**:

1. **«تحت الإهلاك»** — من `capital_asset` (السلوك الحالي، لا تلمسه).
2. **«أصول بلا إهلاك»** — جديد: من `expense` و`purchase` حيث `isCapitalAsset = true` و`deletedAt IS NULL`، **ولا يوجد** صف `capital_asset` مطابق على `(source_type, source_id)` مع `deleted_at IS NULL`.

بطاقة القسم الثاني: الاسم، المبلغ، التاريخ، ومصدره (مصروف/شراء).

**زر «فعّل الإهلاك»**: تحقّق أولاً مما إذا كان `DepreciationPromptModal` قابلاً لإعادة الاستخدام من هنا. **إن كان يتطلّب تغييراً في `addCapitalAsset` أو في منطق الإهلاك — لا تفعل، واكتفِ بالعرض** وأبلغ عن ذلك في تقريرك. العرض وحده يحلّ المشكلة الأساسية.

⚠️ `addCapitalAsset` مُعرَّف بأنه idempotent على `(source_type, source_id)` — لا تكسر ذلك.

---

## المرحلة 2 (الأصغر) — نصّ ثابت للزرّ العائم

في `FinanceClient.tsx` (~السطر 220):

```ts
const addLabel: Record<string, string> = {
  purchases: "مشتريات جديدة", expenses: "مصروف جديد", sales: "مبيعات جديدة", ...
};
```

نصّ الزرّ يتبدّل مع التبويب، فيبدو زرّين مختلفين وهو واحد.

**المطلوب:** في تبويب «مدفوعاتي» يكون النصّ **«تسجيل جديد»** ثابتاً. تبويب «مبيعاتي» يحتفظ بنصّه («مبيعات جديدة») لأنه يستعمل `SaleForm` لا `SmartFinanceForm`.

عند الضغط في «مدفوعاتي» يُفتح `SmartFinanceForm` بوضع افتراضي `expense` — والمستخدم يبدّل الوضع من داخل النموذج.

---

## المرحلة 3 (الأكبر) — دمج «مصاريفي» و«مشترياتي» في «مدفوعاتي»

### 3.أ — استعلام موحَّد

أنشئ `getPayments()` في `src/features/finance/queries.ts` يدمج الجدولين.

**نقطة تقنية مُتحقَّق منها:** الجدولان يستعملان **نفس آلية الـ cursor** — `(createdAt, id)` تنازلياً، بصيغة `"ISO|uuid"`، ويجلبان `limit + 1` لاشتقاق `nextCursor`. راجع `getExpenses` و`getPurchases` قبل الكتابة والتزم بالنمط نفسه.

- استعمل `UNION ALL` على مستوى SQL مع `ORDER BY created_at DESC, id DESC` و`LIMIT n+1` — **لا تجلب الجدولين كاملين وتدمجهما في JS** (يكسر الترقيم ويُحمّل الذاكرة).
- كل صف يحمل حقلاً مميِّزاً `kind: 'expense' | 'purchase'`.
- الحقول المشتركة موحَّدة الأسماء؛ الخاصة بكل نوع تبقى `nullable`.

**الفلاتر المطلوبة على الخادم:**

| الفلتر | السلوك |
|---|---|
| `الكل` | مصاريف + مشتريات (شاملاً الأصول والهدر) |
| `مصاريف` | `expense` فقط، **باستثناء** `isCapitalAsset = true` |
| `مشتريات` | `purchase` فقط، **باستثناء** `isCapitalAsset = true` |
| `أصول` | الجدولان معاً حيث `isCapitalAsset = true` |

**البحث:** يطابق السلوك الحالي — `category`/`description` للمصاريف، و`item`/`supplier` للمشتريات.

**فلتر الفئة الحالي** (`?category=` في `ExpensesTab`) خاصّ بالمصاريف. أبقِه **ظاهراً فقط** عند اختيار رقاقة «مصاريف»، ومخفياً في البقية. وفلتر `?nature=` القائم يبقى كما هو.

### 3.ب — البطاقة الموحَّدة

سطر أول: الاسم + المبلغ. سطر ثانٍ **يتغيّر حسب النوع**:

| النوع | السطر الثاني |
|---|---|
| مصروف | الفئة + شارة التصنيف + التاريخ |
| شراء | `المورد: X · الكمية×سعر الوحدة` + التاريخ |
| أصل رأسمالي | شارة «رأس مال» + التاريخ |
| هدر مخزون | **رمادي** + «خسارة مخزون — بلا دفع» + التاريخ |

استخرج البطاقة في مكوّن واحد يتفرّع على `kind`. أعِد استخدام `AmountText` و`DateText` و`InfoTooltip` القائمة — لا تُعِد بناءها.

**الهدر:** يبقى ظاهراً (لا مفتاح إخفاء)، مميّزاً بلون رمادي، **وللقراءة فقط بلا زر ⋯** — هذا سلوك قائم يجب الحفاظ عليه (`isInventoryWriteoff`).

### 3.ج — التبويبات والتوجيه

- `TABS` في `FinanceClient.tsx` يصير: **«مدفوعاتي» · «مبيعاتي»** (مع بقية التبويبات غير المعنيّة كما هي — `accounts`, `owner`, `opening`).
- التعديل والحذف يبقيان يعملان كما هما لكل نوع (`editExpense` / `editPurchase`).
- **توافق الروابط:** `?tab=expenses` و`?tab=purchases` يجب أن يُوجِّها لـ«مدفوعاتي» مع تفعيل الرقاقة المناسبة — لا تكسر روابط محفوظة أو أزراراً في شاشات أخرى. **ابحث عن كل مَن يبني روابط لهذين التبويبين قبل التغيير.**

---

## المرحلة 4 — تنظيف

بعد استقرار المراحل السابقة: احذف ما صار ميتاً فعلياً (تحقّق بـ grep، لا تفترض). **لا تحذف `SalesTab` ولا أي شيء ما زال مستعملاً.**

---

## 🔒 خطوط حمراء

1. **لا تلمس `src/features/finance/pnl.ts`** ولا أي منطق محاسبي. هذه مهمة عرض بحتة.
2. **لا migration ولا تعديل مخطط.** `pnpm drizzle-kit generate` يجب أن يبقى يطبع «No schema changes, nothing to migrate 😴».
3. **`purchase.total_cents` عمود `GENERATED ALWAYS`** — لا يُكتب يدوياً أبداً.
4. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`/`text-start`/`text-end`. ممنوع `pl-`/`pr-`/`left-`/`right-`/`text-left`/`text-right`.
5. **النقود أعداد صحيحة بالفلس.** `formatFilsToJod` هو المنسّق الوحيد. لا حساب عائم.
6. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ — مرفوض قطعياً (§1 في `CLAUDE.md`).
7. **أهداف اللمس ≥ 44px**، ويُفحص على 360px و390px: لا تمرير أفقي، لا قصّ، لا تراكب.
8. **صفوف الهدر تبقى للقراءة فقط** — لا زر ⋯، لا تعديل، لا حذف.
9. **لا تُسقط أي حقل** يراه المالك اليوم. المورّد والكمية وسعر الوحدة والفئة وشارات التصنيف — كلها تبقى مرئية.

---

## بوابات الفحص — إلزامية قبل كل كومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build                # البوابة الحقيقية
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **`typecheck` وحده لا يكفي** — لا يكشف خرق حدود الخادم/العميل (`"use server"` ناقص). **`pnpm build` هو المعيار.**

⚠️ لا تشغّل `pnpm build` من جذر الـ monorepo — يفشل على حزمة غير ذات صلة.

---

## الاختبار المطلوب

نفّذ ما تستطيع، **وقل صراحةً ما لم تستطع ولماذا**:

1. `/assets` تعرض أصلاً سُجِّل بـ«خصم مرة واحدة» — **اختبار المرحلة 1، الأهم**.
2. الزرّ العائم في «مدفوعاتي» نصّه «تسجيل جديد» ويفتح النموذج الموحَّد.
3. «مدفوعاتي» تعرض المصاريف والمشتريات مرتّبة زمنياً في قائمة واحدة.
4. كل رقاقة فلتر تُنتج المجموعة الصحيحة (راجع الجدول في 3.أ).
5. **الترقيم:** مرّر لأسفل عبر عدّة صفحات — لا تكرار ولا صفوف مفقودة عند حدود الصفحات.
6. التعديل والحذف يعملان للنوعين من القائمة الموحَّدة.
7. صفوف الهدر بلا زر ⋯.
8. `?tab=expenses` و`?tab=purchases` لا يكسران شيئاً.
9. `/reports` → «فحص الآن» → **الكل PASS أو WARN، ولا واحد FAIL**. إن ظهر `IC-1 = FAIL` **أوقف فوراً** ولا ترفع.
10. 360px: بلا تمرير أفقي ولا قصّ، وأهداف اللمس ≥ 44px.

---

## الكومت والرفع

- **لا ترفع إلى أي مستودع بعيد.** المالك يراجع محلياً.
- **كومت منفصل لكل مرحلة** (1 / 2 / 3 / 4) — للمراجعة والتراجع.
- رسائل الكومت بالعربية، تشرح **لماذا** لا ماذا فقط.
- ذيّل كل كومت بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

اذكر صراحةً:
- ما نُفِّذ وما لم يُنفَّذ **ولماذا**.
- مخرجات البوابات الثلاث **حرفياً** (لا «نجحت» فقط).
- أي اختبار من العشرة **لم** تنفّذه ولماذا.
- أي خطّ أحمر اقتربت منه.
- إن قرّرت الاكتفاء بالعرض في زر «فعّل الإهلاك» (المرحلة 1) — قل ذلك.

**لا تدّعِ إنجازاً لم يحدث.** المراجعة ستتحقّق من كل بند مقابل الكود الفعلي: كل استعلام، كل فلتر، كل حقل، ومخرجات البناء. الادّعاء غير الصحيح يُكتشف.

**إن اكتشفت أثناء العمل أن جزءاً من الخطة خاطئ أو يكسر شيئاً — توقّف وأبلغ بدل الالتفاف حوله.**
