# 🔴 تحقيق عاجل — أخطاء 500 على الإنتاج بعد جولة الأداء

> ## 🛑 بحث فقط — لا إصلاح
>
> **لا تُعدّل ملفاً. لا تكتب كوداً. لا تُنشئ كومت.**
>
> اقرأ الكود بحرّية (Read / grep). المطلوب **تقرير تشخيص** يُعرَض على المالك.
>
> **النظام معطّل الآن** — المالك يريد رأياً ثانياً قبل التنفيذ لئلا يزيد إصلاح خاطئ الطين بلّة.

---

## 1. العَرَض

بعد نشر المراحل الأربع لجولة الأداء، التطبيق **لا يعمل على الإنتاج**:

- لوحة التحكم تعرض «خطأ في الشبكة — حدث خطأ أثناء تحميل بيانات لوحة القيادة».
- الـConsole يُظهر **عشرات** `POST ... 500 (Internal Server Error)`.
- **المالك يؤكّد أن الخلل ليس في لوحة التحكم وحدها — بل في أغلب الشاشات.**

---

## 2. أدلّة مقيسة (لا تخمين)

### أ. قاعدة البيانات

| القياس | القيمة |
|---|---|
| `max_connections` | **60** |
| اتصالات نشطة وقت الفحص | **22** |
| المتاح فعلياً | **~38** |

### ب. سلوك متقطّع — دليل حاسم

`getFinancialSummary` **فشلت ثم نجحت** في استدعاءين متتاليين بنفس المعاملات وبلا أي تغيير في الكود:

```
❌ getFinancialSummary: Failed query: select sum("cash_movement"."amount_cents")
   from "cash_movement" inner join "account" ...
✅ (نفس الاستدعاء بعد دقيقة) نجح
```

**الفشل المتقطّع ينفي «شيفرة مكسورة» ويرجّح استنفاد مورد.**

### ج. الدوال فرادى تعمل

`getDashboardStats` · `getCashSummary` · `getAverageMonthlySpend` · `getAllComponentStocks` — **كلها نجحت** عند استدعائها منفردة.

### د. الحزمة كاملة تتجاوز المهلة

`getDashboardBundle` تجاوزت مهلة 4 دقائق **مرّتين** بلا نتيجة ولا خطأ صريح.

### هـ. إعداد العميل — `max` غير محدَّد

`src/lib/db/client.ts`:

```ts
const client = postgres(env.DATABASE_URL, {
  prepare: false,
  connect_timeout: 30,
  idle_timeout: 30,
  max_lifetime: 60 * 30,
});
```

**لا يوجد `max`** — أي لا سقف صريح لعدد الاتصالات المتزامنة في التجمّع.

---

## 3. الفرضية القائمة — قيّمها ولا تُسلّم بها

**الادّعاء:** المراحل 1 و3 معاً ضاعفت التوازي حتى تجاوز حدّ الاتصالات.

الحساب المزعوم:

| الطبقة | قبل | بعد |
|---|---|---|
| `computeOperatingPnl` | 6 استعلامات **متسلسلة** | 6 **متوازية** (`Promise.all`) |
| `getMonthlyProfit` | 6 أشهر متسلسلة | 6 أشهر **متوازية** × 6 استعلامات = **36 معاً** |
| `getDashboardBundle` | 11 استدعاءً من المتصفّح **بالتتابع** | 11 **معاً على الخادم** |

**الاستنتاج المزعوم:** عشرات الاتصالات دفعة واحدة على حدّ 60 (منها 22 مشغولة) → Supabase يرفض الزائد → 500.

⚠️ **هذه فرضية لم تُختبر على Vercel.** أكّدها أو انقضها بدليل من الكود.

---

## 4. ما يجب أن يفسّره أي تشخيص صحيح

1. **لماذا الخطأ في أغلب الشاشات** لا لوحة التحكم وحدها؟
   ⚠️ الكتالوج والطلبات والمالية **لا تستعمل الحزمة**. إن كان السبب الحزمة وحدها، فلماذا تُصاب شاشات أخرى؟ **هذا أهمّ سؤال في التحقيق.**
2. **لماذا الفشل متقطّع** لا دائم؟
3. **لماذا لم يظهر محلياً؟** الاختبار من جهاز المالك نجح (12 استعلاماً متوازياً بلا فشل).
4. **لماذا تجاوزت الحزمة المهلة** بلا خطأ صريح؟

---

## 5. مسارات تستحقّ الفحص

**افحصها كلها، ولا تكتفِ بالأولى:**

| # | المسار |
|---|---|
| 1 | **تجمّع الاتصالات** — `max` غير محدَّد. كم اتصالاً يفتح `postgres.js` افتراضياً؟ وكيف يتصرّف عند تجاوز حدّ الخادم؟ |
| 2 | **Supavisor pooler (منفذ 6543)** — له حدوده الخاصة غير `max_connections`. هل يرفض أم يُصفّ؟ |
| 3 | **Vercel serverless** — كل استدعاء قد يُنشئ نسخة جديدة من `client.ts`، فتتضاعف التجمّعات. هل هذا يحدث؟ |
| 4 | **`connect_timeout: 30`** (رُفع في المرحلة 2) — هل يُطيل احتجاز الاتصالات الفاشلة فيفاقم الاستنفاد؟ |
| 5 | **دالة بعينها مكسورة** — راجع تغييرات المراحل الأربع بحثاً عن خطأ منطقي، لا مورد. |
| 6 | **`/api/health`** (أُضيف في المرحلة 4) — هل يفتح اتصالاً ولا يُغلقه؟ الـlazy import قد يُنشئ عميلاً منفصلاً. |
| 7 | **سبب خارجي تماماً** — تغيّر في Supabase، أو حدّ خطة، أو انقطاع. ما الذي ينفي ذلك أو يؤكّده؟ |

---

## 6. الكومتات المعنيّة

```
2cd9182  المرحلة 4 — N+1 الكتالوج + /api/health
8fa1e20  المرحلة 3 — getDashboardBundle
9bd5ed4  المرحلة 2 — connect_timeout 30 + staleTime + شريط التنبيه
a66a5c9  المرحلة 1 — Promise.all في pnl.ts و getMonthlyProfit
```

آخر حالة معروفة سليمة: **قبل `a66a5c9`**.

**اقرأ `git diff` لهذه المراحل** وابحث عمّا قد يُنتج 500 — سواء استنفاد مورد أو خطأ منطقي.

---

## 7. المطلوب في تقريرك

1. **حكمك على فرضية §3:** مؤكَّدة / مبالغ فيها / خاطئة — **بدليل من الكود** (ملف:سطر).
2. **تفسيرك للأسئلة الأربعة في §4** — خصوصاً الأول (لماذا شاشات أخرى؟).
3. **نتيجة فحص المسارات السبعة** في §5 — ولو بنفي بعضها.
4. **السبب الجذري الذي ترجّحه**، ومستوى ثقتك فيه، وما يدعمه.
5. **الإصلاح الذي تقترحه** — وصفاً لا كوداً. ولماذا هو الأنسب.
6. **هل يكفي إصلاح واحد أم يلزم أكثر؟**
7. **ما إذا كان التراجع (revert) أسلم** من الإصلاح المباشر — ولأي مرحلة تحديداً.
8. **ما لم تستطع التحقّق منه ولماذا.**

⚠️ **بيئتك لا تملك `DATABASE_URL`** (ثبت في المراحل الأربع). **لا تحاول الاتصال.** اعتمد على قراءة الكود والأدلّة أعلاه، وقل صراحةً ما لم تستطع فحصه.

---

## 8. تحذيران

**لا تُصلح.** حتى لو بدا السبب واضحاً. المالك يريد **رأياً مستقلاً** ليقارنه بتشخيص آخر قبل التنفيذ.

**لا تُجامل التشخيص القائم.** إن كانت الفرضية في §3 خاطئة أو ناقصة، **قل ذلك بوضوح** — قيمة هذه الاستشارة كلّها في استقلال رأيك. وإن رأيت سبباً لم يُذكر إطلاقاً، فهو أهمّ ما قد تُقدّمه.

**ولا تدّعِ فحصاً لم تقم به.** إن لم تفحص ملفاً، قل إنك لم تفحصه.
