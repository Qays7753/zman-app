# المرحلة 3 من 4 — نقطة تجميع واحدة للوحة التحكم

> ⚠️ **نفّذ هذه المرحلة فقط. توقّف عند نهايتها وانتظر إذن المالك.**
> **المرحلتان 1 و2 أُنجزتا واعتُمدتا** — لا تُعِد عملهما.
> هناك مرحلة رابعة — **لا تبدأها**.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً — كلّه**، خصوصاً §2 (النموذج المالي وLOCKED-6)، §3، §10، §11.

المستخدم **شخص واحد** — مالك ورشة، على الهاتف 95% من الوقت.

### التشخيص — مقيس فعلياً على قاعدة الإنتاج

| القياس | القيمة |
|---|---|
| زمن الشبكة لكل رحلة (RTT) | **~150 ms** |
| استدعاءات لوحة التحكم | **7 هوكس منفصلة** |

**حجم البيانات: 83 حركة صندوق · 16 مصروفاً · 37 شراءً · 27 مبيعة.**

### 🔴 الفكرة المحورية — افهمها قبل أن تكتب سطراً

قاعدة البيانات **لا تحسب شيئاً**. `select 1` التافه يستغرق 150ms — الخادم يعالج في أقلّ من مللي ثانية، **والباقي كلّه شبكة**.

كل هوك في لوحة التحكم = **رحلة مستقلّة** من متصفّح المالك إلى الخادم.

```
اليوم:  المتصفّح → 7 رحلات مستقلّة (7 × 150ms + معالجة)
بعده:   المتصفّح → رحلة واحدة → كل الاستعلامات بالتوازي داخل الخادم (~0ms بينها)
```

**الاستعلامات داخل الخادم شبه مجانية** لأن الخادم بجوار قاعدة البيانات. الكلفة الحقيقية هي الرحلة من المتصفّح.

> **هذه أهمّ مرحلة في المشروع من ناحية الأداء.**

---

## 🔒 الخطّ الأحمر الأول — الأرقام المالية

`pnl.ts` محروس بـ**LOCKED-6**: تعريف الربح يجب أن يتطابق في ثلاثة مواضع (لوحة التحكم · التقارير · الرسم الشهري)، و`IC-13` يفحصه وقت التشغيل.

**إلزامي:**
1. **سجّل الأرقام قبل التغيير** — الربح، النقد، إجمالي الأصول، `equityDriftCents`.
2. نفّذ التغيير.
3. **سجّلها بعده وأثبت التطابق بالفلس.**
4. شغّل فحص السلامة → **صفر FAIL**.

**إن اختلف رقم واحد — تراجع فوراً وأبلغ. لا تُكوّت.**

ℹ️ `IC-12 = WARN` (أرصدة مخزون سالبة) **موجود مسبقاً** — تجاهله.

---

## المطلوب في هذه المرحلة

### الهدف

دالة **واحدة** `getDashboardBundle(params)` في `src/features/dashboard/queries.ts` تُنفّذ كل استعلامات لوحة التحكم في `Promise.all` **داخل الخادم** وتُعيدها دفعة واحدة، وهوك واحد `useDashboardBundle` يحلّ محلّ السبعة في `DashboardClient`.

### الاستدعاءات السبعة الحالية

افحص `DashboardClient.tsx` بنفسك وأحصِ ما تجده فعلياً. المرصود مبدئياً:
`useFinancialSummary` (مرّتين — الفترة المختارة + كل الفترات) · `useDashboardStats` · `useCashSummary` · `useAccountBalances` · `useOpeningBalance` · `useAverageMonthlySpend` · `useFinancialPosition` · `useMonthlyProfit` · `useInventoryValuation` · `useCapitalAssets`

⚠️ **العدد الفعلي قد يختلف — اعتمد على فحصك لا على هذه القائمة.**

### متطلّبات إلزامية

1. **لا تحذف الدوال الأصلية.** قد تُستعمل في شاشات أخرى (`/reports`، `/assets`…). **تحقّق بـgrep قبل أي حذف** — الحزمة تستدعيها، لا تستبدلها.

2. **`getFinancialSummary` يُستدعى مرّتين** في `DashboardClient` (الفترة + كل الفترات). أدرج الاثنين في الحزمة **بلا تكرار العمل المشترك** إن أمكن — وإن لم يمكن بأمان، أبقِ الاستدعاءين وقل ذلك.

3. **حالات التحميل والخطأ يجب أن تبقى تعمل.** حزمة واحدة = حالة تحميل واحدة. تأكّد أن الواجهة لا تنكسر.

   🔴 **حماية عمل المرحلة 2 — الأخطر في هذه المرحلة.** `DashboardClient` يحوي الآن منطقاً أُنجز واعتُمد:
   - `hasCachedData` (~السطر 169) — يفحص `summary || cashSummary || position || summaryAllTime`
   - `isAnyError` (~السطر 144) — يجمع حالات الخطأ
   - شريط التنبيه «تعذّر التحديث — البيانات قد تكون قديمة» (~السطر 255) وزرّ إعادة المحاولة
   - `ErrorState` عند `isAnyError && !hasCachedData`

   استبدال الهوكس بحزمة واحدة **يكسر هذا المنطق حتماً** إن لم تُكيّفه. المطلوب:
   - `hasCachedData` يعكس وجود بيانات الحزمة المخزَّنة.
   - `isAnyError` يعكس حالة خطأ الحزمة.
   - **الشريط و`ErrorState` يعملان كما هما بالضبط** — لا تحذف أيّاً منهما.
   - ⚠️ **يبقى ممنوعاً عرض صفر أو رقم افتراضي** مكان بيانات غائبة.

   **إن رأيت أن الحزمة تُصعّب هذا المنطق — توقّف وأبلغ بدل حذفه أو تبسيطه.**

4. **لا تُغيّر أي حساب.** الحزمة تُجمّع فقط — لا تُعيد تعريف شيء ولا تحسب شيئاً جديداً.

5. **إن فشل استعلام واحد داخل الحزمة**، قرّر بوعي: هل تفشل الحزمة كلها أم تُعيد ما نجح؟ **صرّح بقرارك في التقرير.** ⚠️ لا تُعِد صفراً أو قيمة افتراضية مكان ما فشل — ذلك رقم مالي مُفبرك.

6. **أعِد قياس الحزمة مقابل السبعة المنفصلة** وأثبت الفائدة بالأرقام.

### حدّ النطاق

⚠️ **اقتصر على لوحة التحكم.** الكتالوج (~21 استدعاءً) والطلبات (~15) في المرحلة 4 — **لا تلمسهما**.

---

## 🔒 خطوط حمراء

1. **الأرقام المالية لا تتغيّر بفلس.** أثبت ذلك قبل/بعد.
2. **لا تُغيّر تعريف الربح** (LOCKED-6). الحزمة تُجمّع لا تحسب. إن اضطررت لتغيير منطق — **توقّف وأبلغ**.
3. **لا migration ولا تعديل مخطط.**
4. **لا تُشغّل شيئاً على قاعدة الإنتاج.**
5. **لا أرقام مالية مُفبركة** ولا قيم افتراضية مكان بيانات فاشلة.
6. **لا تحذف دالة** قبل التحقّق بـgrep أنها بلا مستدعٍ.
7. **لا تلمس** `getPayments` ولا `getReceivables` — أُصلحتا مؤخّراً بعد أعطال وقت تشغيل.
8. **لا تلمس** الكتالوج ولا الطلبات ولا `useComponentStock` — **مرحلة تالية**.
9. **لا تلمس** `lib/db/client.ts` ولا `query-provider.tsx` — أُنجزا في المرحلة 2.
10. **RTL — خصائص منطقية فقط.** أهداف اللمس ≥44px. لا تمرير أفقي.
11. **لا تكسر التوافق العكسي** لأي معامل رابط.

---

## بوابات الفحص — قبل الكومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **البوابات الثلاث لا تكشف أخطاء وقت التشغيل.** جولة سابقة اجتازتها كلها وكان تبويب «مدفوعاتي» **لا يفتح إطلاقاً** (خطأ SQL). **شغّل الحزمة فعلياً — لا تفترض صحّتها من البناء.**

---

## القياس المطلوب

⚠️ **معلومة مؤكَّدة من المرحلتين 1 و2: بيئتك لا تملك `DATABASE_URL`** — ملف `.env` في جذر المستودع لا في `artifacts/zman-app`، ولن تستطيع تشغيل أي قياس ولا التحقّق من الأرقام المالية.

**لا تُضيّع الوقت في محاولات اتصال.** قل صراحةً في تقريرك: «تعذّر القياس — لا وصول لقاعدة البيانات»، **والمالك سيقيس بنفسه ويتحقّق من الأرقام قبل الرفع**.

**ما يلزمك بدلاً من القياس:**
- **أحصِ عدد رحلات الشبكة** قبل وبعد (عدد الاستدعاءات المنفصلة مقابل واحد).
- **أثبت بالكود** أن الحزمة تستدعي نفس الدوال بنفس المعاملات، وأن الحساب لم يتغيّر.
- **اذكر أي موضع قد يتغيّر فيه رقم** — ولو احتمالاً — ليفحصه المالك تحديداً.

---

## الاختبار المطلوب

**قل صراحةً ما لم تنفّذه ولماذا. لا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.**

1. **الأرقام المالية متطابقة بالفلس** قبل/بعد.
2. فحص السلامة → **صفر FAIL** (`IC-12 = WARN` مقبول ومسبق).
3. **لوحة التحكم تعرض نفس الأرقام تماماً** — كل بطاقة وكل رقم.
4. حالة التحميل تعمل (لا شاشة فارغة ولا وميض غريب).
5. **معالجة الكاش القديم من المرحلة 2 ما زالت تعمل.**
6. الرسم البياني الشهري يعرض نفس القيم بنفس الترتيب.
7. 360px: لا تمرير أفقي، لا كسر في التخطيط.

---

## الكومت

- **لا ترفع إلى أي مستودع بعيد.**
- كومت واحد لهذه المرحلة.
- رسالة بالعربية تشرح **لماذا**، مع **أرقام القياس**.
- ذيّلها بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. **جدول رحلات الشبكة قبل/بعد** — كم استدعاءً كان، وكم صار.
2. **إثبات بالكود أن الحساب لم يتغيّر** — نفس الدوال، نفس المعاملات.
3. **عدد الاستدعاءات الفعلي** الذي وجدته في `DashboardClient` (وليس المرصود مبدئياً).
4. **كيف حافظت على منطق المرحلة 2** — `hasCachedData` و`isAnyError` والشريط و`ErrorState` (§3).
5. **قرارك في فشل استعلام داخل الحزمة** (§5) ولماذا.
6. مخرجات البوابات الثلاث **حرفياً**.
7. أي دالة أصلية أبقيتها ولماذا.
8. **أي موضع قد يتغيّر فيه رقم** — ولو احتمالاً — ليفحصه المالك تحديداً.
9. أي اختبار لم تنفّذه ولماذا.

---

## 🛑 عند الانتهاء

**توقّف. لا تبدأ أي عمل آخر.**

اكتب في نهاية تقريرك: **«المرحلة 3 اكتملت — بانتظار إذن المالك للمرحلة 4.»**

**إن اكتشفت أن جزءاً من الخطة خاطئ أو يُهدّد الأرقام — توقّف وأبلغ فوراً بدل الالتفاف حوله.**
