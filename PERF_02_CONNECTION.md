# المرحلة 2 من 4 — الاتصال والكاش وشاشة الخطأ

> ⚠️ **نفّذ هذه المرحلة فقط. توقّف عند نهايتها وانتظر إذن المالك.**
> **المرحلة 1 (توازي الاستعلامات) أُنجزت واعتُمدت** — لا تُعِد عملها ولا تُراجعها.
> هناك مرحلتان تاليتان — **لا تبدأهما**.

---

## 0. السياق

monorepo بـ pnpm. التطبيق: `artifacts/zman-app` (Next.js 15 App Router).
**اقرأ `CLAUDE.md` في الجذر أولاً**، خصوصاً §2 (النموذج المالي)، §10 (RTL والهاتف)، §11 (بوابات الفحص).

المستخدم **شخص واحد** — مالك ورشة، **على الهاتف 95% من الوقت** (360–390px).

### الشكوى

> «النظام بطيء، وخاصة عند فتحه. وأحياناً يعطي: حدث خطأ أثناء تحميل البيانات.»

**هذه المرحلة تعالج الشطر الثاني: رسالة الخطأ.**

### التشخيص — مقيس فعلياً

| القياس | القيمة |
|---|---|
| الاتصال البارد الأول بقاعدة البيانات | **~1,320 ms** |
| `connect_timeout` الحالي | **10 ثوانٍ** |

الاتصال البارد قد يتجاوز المهلة عند سبات Supabase، فيفشل الطلب وتظهر رسالة الخطأ.

---

## المطلوب في هذه المرحلة

### 2.أ — `src/lib/db/client.ts`

```ts
connect_timeout: 10,   // ← ارفعه إلى 30
// @ts-expect-error postgres-js types may not include retry in Options
retry: 1,              // ← احذف السطرين معاً
```

**`retry` غير موجود في `postgres.js` إطلاقاً** — سطر لا يفعل شيئاً، وتعليق `@ts-expect-error` فوقه يوحي بحماية وهمية. احذف السطرين.

🔴 **لا تُفعّل `prepare: true`** — الاتصال عبر Supavisor pooler (منفذ 6543) و**لا يدعم Prepared Statements**. تفعيله يكسر الاتصال بالكامل.

**لا تُغيّر** `idle_timeout` ولا `max_lifetime` — لهما سبب موثَّق في التعليقات.

### 2.ب — `src/providers/query-provider.tsx`

`staleTime: 30 * 1000` → **5 دقائق**.

**السبب:** مع `refetchOnWindowFocus: true`، كل عودة للتطبيق تُطلق موجة استعلامات كاملة. 30 ثانية قصيرة جداً لمستخدم واحد ببيانات لا تتغيّر إلا بفعله.

**لا تُغيّر** `gcTime` (24 ساعة) ولا الكاش الدائم في `localStorage` — مضبوطان ومتوافقان.
**لا تُغيّر** `retry: 3` ولا `retryDelay` — يخدمان الاتصال البارد.

### 2.ج — لا شاشة خطأ ما دام هناك كاش 🔴

اليوم `DashboardClient` يعرض `ErrorState` عند أول فشل — **حتى لو كانت البيانات محفوظة في `localStorage`** (والكاش الدائم مُفعَّل فعلاً بـ`maxAge` 24 ساعة).

**المطلوب:** إن وُجد كاش، اعرضه **دائماً** بدل شاشة الخطأ، مع **شريط تنبيه مرئي**:

> «تعذّر التحديث — البيانات قد تكون قديمة»

**متطلّبات:**
- **الشريط إلزامي وليس تجميلياً.** عرض رقم مالي قديم بلا تنبيه صريح يخالف مبدأ النظام («رقم واحد يثق به المالك»).
- **بلا كاش، تبقى `ErrorState` كما هي** — لا تحذفها.
- الشريط يختفي فور نجاح التحديث.
- ⚠️ **لا تعرض رقماً مُفبركاً ولا صفراً افتراضياً** عند غياب البيانات — إمّا كاش حقيقي مع تنبيه، أو شاشة خطأ.

---

## 🔒 خطوط حمراء

1. **لا تلمس أي منطق محاسبي** — لا `pnl.ts`، لا `reports/actions.ts`، لا `integrityCheck.ts`. هذه مرحلة اتصال وواجهة.
2. **لا `prepare: true`** — يكسر Supavisor pooler.
3. **لا migration ولا تعديل مخطط.** `drizzle-kit generate` يبقى يطبع «No schema changes».
4. **لا تُشغّل شيئاً على قاعدة الإنتاج.**
5. **لا أرقام مالية مُفبركة** ولا بيانات وهمية عند الفراغ أو الخطأ (§1 في `CLAUDE.md`) — هذا الخطّ **الأهمّ** في §2.ج.
6. **لا تلمس** `dashboard/queries.ts` ولا `finance/queries.ts` — **مرحلة تالية**.
7. **لا تُنشئ نقطة تجميع ولا تُغيّر عدد الهوكس في `DashboardClient`** — **مرحلة تالية**. اقتصر على معالجة حالة الخطأ.
8. **RTL — خصائص منطقية فقط:** `ps-`/`pe-`/`ms-`/`me-`/`start-`/`end-`. ممنوع `pl-`/`pr-`/`left-`/`right-`.
9. **أهداف اللمس ≥ 44px**، 360px و390px، لا تمرير أفقي.

---

## بوابات الفحص — قبل الكومت

```bash
cd artifacts/zman-app     # ← من هنا، لا من جذر الـ monorepo
pnpm build
pnpm typecheck
pnpm drizzle-kit generate # يجب أن يطبع "No schema changes, nothing to migrate 😴"
```

⚠️ **البوابات لا تكشف أخطاء وقت التشغيل.** جولة سابقة اجتازتها كلها وكان تبويب «مدفوعاتي» لا يفتح إطلاقاً.

---

## الاختبار المطلوب

**قل صراحةً ما لم تنفّذه ولماذا. لا تعلّم ✅ على اختبار استنتجته من الكود بدل تشغيله.**

1. التطبيق يتّصل ويعمل بعد رفع `connect_timeout`.
2. حذف `retry` لم يكسر شيئاً.
3. **الكاش القديم يُعرض مع شريط التنبيه** بدل شاشة الخطأ — اشرح كيف اختبرت ذلك.
4. **بلا كاش، تظهر `ErrorState`** كما كانت.
5. شريط التنبيه يختفي عند نجاح التحديث.
6. **لا يظهر أي رقم مالي بلا مصدر حقيقي.**
7. 360px: الشريط لا يكسر التخطيط، لا تمرير أفقي.

---

## الكومت

- **لا ترفع إلى أي مستودع بعيد.**
- كومت واحد لهذه المرحلة (أو اثنان: الاتصال / الواجهة).
- رسالة بالعربية تشرح **لماذا**.
- ذيّلها بـ:
  ```
  Co-Authored-By: Gemini <noreply@google.com>
  ```

## تقريرك النهائي

1. ما نُفِّذ وما لم يُنفَّذ **ولماذا**.
2. مخرجات البوابات الثلاث **حرفياً**.
3. **كيف اختبرت حالة الكاش القديم** (§2.ج) — أو قل صراحةً إنك لم تستطع.
4. صياغة شريط التنبيه النهائية.
5. أي اختبار من السبعة لم تنفّذه ولماذا.

---

## 🛑 عند الانتهاء

**توقّف. لا تبدأ أي عمل آخر.**

اكتب في نهاية تقريرك: **«المرحلة 2 اكتملت — بانتظار إذن المالك للمرحلة 3.»**

**إن اكتشفت أن جزءاً من الخطة خاطئ — توقّف وأبلغ بدل الالتفاف حوله.**
