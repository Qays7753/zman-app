# تقرير حادثة — أخطاء 500 على الإنتاج (Zman)

> **الغرض:** عرض كامل ودقيق على مستشار خارجي.
> **الحالة عند كتابة التقرير:** المشكلة **ما زالت قائمة** بحسب المالك، رغم إصلاح سبب مؤكَّد.
> **التاريخ:** 18 آب 2026.

---

## 1. النظام

| البند | القيمة |
|---|---|
| التطبيق | Next.js 15.5.19 (App Router)، TypeScript |
| البنية | monorepo بـ pnpm؛ التطبيق في `artifacts/zman-app` |
| الاستضافة | Vercel — الخطة المجانية · مُعدّ عبر Dashboard (**لا `vercel.json`**) |
| النطاق | `zman-app-api-server.vercel.app` |
| قاعدة البيانات | Supabase PostgreSQL عبر **Supavisor pooler** (منفذ 6543) |
| ORM | Drizzle + `postgres.js` (`prepare: false` لأن الـ pooler لا يدعم Prepared Statements) |
| المصادقة | كوكي `zman_session` مقابل `PASSCODE` في middleware |
| المستخدمون | **شخص واحد** (مالك ورشة)، على الهاتف 95% من الوقت |
| حجم البيانات | 83 حركة صندوق · 16 مصروفاً · 37 شراءً · 27 مبيعة — **صغير جداً** |

---

## 2. العَرَض

- لوحة التحكم تعرض `ErrorState`: «خطأ في الشبكة — حدث خطأ أثناء تحميل بيانات لوحة القيادة».
- الـConsole: **عشرات** `POST https://zman-app-api-server.vercel.app/ 500 (Internal Server Error)`.
- المالك يؤكّد أن الخلل **في أغلب الشاشات لا لوحة التحكم وحدها**.
- **المالك يؤكّد أن الأعطال كانت موجودة قبل جولة تحسين الأداء** — وهذا نفى الفرضية التي بُني عليها التشخيص الأول.
- المالك **لم يُغيّر أي شيء على Vercel** (لا متغيّرات بيئة ولا إعدادات).

---

## 3. السبب المؤكَّد الذي وُجد وأُصلح

### الدليل — من سجلّات Vercel (نصّ حرفي، متكرّر عشرات المرّات)

```
Error: A "use server" file can only export async functions, found object.
Read more: https://nextjs.org/docs/messages/invalid-use-server-value
    at c (.next/server/chunks/144.js:1:32567)
    at 95505 (.next/server/chunks/839.js:349:57886)
    at c (.next/server/webpack-runtime.js:1:189)
    at 19570 (.next/server/chunks/805.js:1:6401)
    at Object.c [as require] (.next/server/webpack-runtime.js:1:189)
    at async m (.next/server/app/(app)/page.js:1:12607) {
  digest: '1457504992'
}
```

**سياق السجلّ:** `type: "cache"` · `function: "/"` · `level: "error"` · `environment: "production"` · `branch: "main"` · `deploymentId: dpl_DHmnXKQ8VPt25eCZvHmKh4o5bmgx`.

### الكود المخالف

`artifacts/zman-app/src/features/finance/actions.ts` — ملف يبدأ بـ`"use server"`:

```ts
export const DEFAULT_EXPENSE_CATEGORIES = [
  "رواتب", "إيجار", "كهرباء ومياه", "نقل وتوصيل",
  "تعبئة وتغليف", "صيانة وأدوات", "حوافز", "أخرى",
] as const;
```

**القاعدة:** ملفات `"use server"` في Next.js تُصدّر **دوال `async` فقط**. تصدير أي قيمة أخرى (مصفوفة/كائن/ثابت) يرمي **وقت التشغيل** ويُسقط الصفحة.

**متى أُضيف:** كومت `c07d27d` (جولة توحيد فئات المصاريف) — **قبل** جولة الأداء. هذا يفسّر تأكيد المالك أن الأعطال أقدم منها.

### الإصلاح المطبَّق

حُذفت كلمة `export` فقط (الثابت مستعمل داخل الملف نفسه في السطر 1860، فلا مستورد خارجي).

**الكومت:** `5f40818` — **مرفوع للمستودعين** (`origin` و`qays`)، ومؤكَّد بـ`git ls-remote`.

**فحص شامل بعده:** لا يوجد أي `export const/class/enum/let/var/default` متبقٍّ في أي ملف `"use server"` في المشروع. (الـ`interface` و`type` مسموحان لأنهما يختفيان عند البناء.)

---

## 4. لماذا لم يُكتشف مبكراً — أربع طبقات خدعت التشخيص

| الفحص | النتيجة | لماذا خدع |
|---|---|---|
| `pnpm build` | ✅ ينجح | القيد يُفحص **وقت التشغيل** لا البناء |
| `pnpm typecheck` | ✅ ينجح | ليس خطأ أنواع |
| تشغيل الدوال مباشرة من سكربت Node | ✅ تنجح | يتجاوز طبقة Server Actions تماماً |
| `GET /api/health` | ✅ 200 | لا يستورد `finance/actions.ts` |

**النتيجة:** بدا أن قاعدة البيانات والاتصال وVercel كلها سليمة — وهي فعلاً سليمة. الخلل كان في تحميل وحدة (module) واحدة عند تنفيذ Server Action.

---

## 5. فرضيات فُحصت ونُفيت بالقياس الفعلي

**كلها نُفيت قبل الوصول للسجلّات. مذكورة لمنع إعادة فحصها.**

| # | الفرضية | كيف نُفيت |
|---|---|---|
| 1 | استنفاد اتصالات قاعدة البيانات | `max_connections = 60`، النشط **13–22** فقط |
| 2 | الحمل المتوازي يكسر Supabase | **60 استعلاماً دفعة واحدة نجحت كلها** في 1,970ms |
| 3 | تعليق (deadlock) بين معاملة واستعلامات | **معاملة + 50 استعلاماً متوازياً نجحت كلها** في 1,755ms |
| 4 | `postgres.js` بلا سقف `max` | السقف الافتراضي **10** (مُتحقَّق بالتشغيل) — الزائد ينتظر ولا يُرفض |
| 5 | متغيّرات بيئة ناقصة على Vercel | `/api/health` يتصل بقاعدة البيانات وينجح |
| 6 | Vercel لا يصل لقاعدة البيانات | **5 استدعاءات متتالية، كلها 200**، متوسط 0.5s |
| 7 | جولة الأداء هي السبب | المالك أكّد أن الأعطال **سبقتها**؛ والتراجع عنها لم يُصلح شيئاً |
| 8 | النطاق معطّل | `GET` و`POST` يُرجعان 307 (توجيه لتسجيل الدخول) في 0.5s |
| 9 | Service Worker يعترض | `public/sw.js` شبكة-أولاً بلا precache؛ ولا يتعامل إلا مع GET |

**قياسات مرجعية على قاعدة الإنتاج:**
- `select 1` (استعلام تافه): **~150 ms** — أي أن الكلفة **شبكة** لا معالجة.
- الاتصال البارد الأول: **~1,320 ms**.
- الخادم يعالج في **أقلّ من مللي ثانية**.

---

## 6. ما فُعل بالترتيب الزمني

| # | الإجراء | النتيجة |
|---|---|---|
| 1 | فحص الاستعلامات الجديدة (`getPayments`, `getAllComponentStocks`) بالتشغيل | تعمل — نُفيت |
| 2 | قياس حدود الاتصالات والحمل المتوازي | سليمة — نُفيت |
| 3 | **تراجع** عن مراحل الأداء 1 و3 و4 (`f3716a2`) | **لم يُصلح شيئاً** |
| 4 | إعادة `connect_timeout` من 30 إلى 10 | تحسين احترازي فقط |
| 5 | **إلغاء التراجع** واستعادة المراحل الأربع (`9db7892`) | بعد تأكيد المالك أن الأعطال أقدم |
| 6 | فحص `/api/health` والنطاق من الخارج | يعملان — نُفيا |
| 7 | **قراءة سجلّات Vercel** | **كشفت السبب فوراً** |
| 8 | حذف `export` من الثابت (`5f40818`) + رفع | البوابات الثلاث ✓ |

⚠️ **الخطوة 3 كانت خطأ منهجياً:** بُنيت على افتراض غير مُتحقَّق منه (أن الأعطال بدأت بعد جولة الأداء)، ولم يُسأل المالك قبلها. أُلغيت بالكامل.

---

## 7. 🔴 الحالة الآن — المشكلة ما زالت قائمة

**المالك يؤكّد استمرار الخطأ بعد الإصلاح والنشر.**

ما تحقّقت منه بعد الرفع:
- الكومت `5f40818` مرفوع للمستودعين ✓
- الإصلاح موجود في الكود (`const` بلا `export`) ✓
- `GET /api/health` يُرجع 200 ✓

**ما لم يُتحقَّق منه بعد:**
- هل التقط Vercel النشر الجديد فعلاً؟
- هل يظهر **نفس** الخطأ في السجلّات الآن، أم خطأ مختلف؟
- هل السبب مركّب (سببان أو أكثر بنفس العَرَض)؟

### فرضيات مطروحة للمستشار

1. **النشر لم يكتمل** أو ما زال يخدم بناءً قديماً (السجلّ القديم يُظهر `deploymentId: dpl_DHmnXKQ8VPt25eCZvHmKh4o5bmgx` — يجب مقارنته بالنشر الحالي).
2. **كاش Vercel:** السجلّ يُظهر `type: "cache"` و`vercelCache: "BYPASS"` على الطلبات الفاشلة. هل يُخدَم إخراج مبنيّ قديم؟
3. **Service Worker على جهاز المالك** يخدم نسخة قديمة (`sw.js` مسجَّل، والمتصفّح قد يحتفظ بالنسخة السابقة).
4. **سبب ثانٍ لم يُكتشف** — نفس العَرَض بمنشأ مختلف.
5. **نشر Preview لفروع قديمة** يتداخل: السجلّ يُظهر نشرات نشطة لفروع `v0/dashboard-feedback-a062cf00` و`zman-finance-upgrade` و`claude/bottom-bar-disappearing-2ll603` وغيرها.

---

## 8. ملاحظات قد تكون ذات صلة

**أ. اسم المشروع مضلِّل:** النطاق `zman-app-api-server` رغم أن التطبيق **ليس API server** بل تطبيق Next.js كامل. لم يُتحقَّق من سبب التسمية.

**ب. فروع Preview نشطة كثيرة** على نفس المشروع (`environment: "preview"` في السجلّ) — ستة فروع على الأقل. أحدها (`v0/dashboard-feedback-a062cf00`) موثَّق في `CLAUDE.md` بأنه **يحقن أرقاماً مالية مفبركة** ويُمنع دمجه.

**ج. الـmiddleware يُغلق بأمان:** إن لم يُضبَط `PASSCODE`، **كل** الطلبات تُعاد لتسجيل الدخول. لكن الطلبات الفاشلة تُرجع **500 لا 307** — أي أنها تجاوزت المصادقة ثم فشلت داخل الخادم.

**د. المناطق:** السجلّ يُظهر تنفيذاً في `fra1` و`iad1` و`sfo1` لنفس الطلب (middleware/cache/static). قاعدة البيانات في `eu-central-1` (فرانكفورت).

**هـ. الـSW:** `public/sw.js` بسيط — شبكة أولاً، بلا precache، ويتعامل مع `GET` فقط (الطلبات الفاشلة `POST` فلا يمسّها).

---

## 9. الأسئلة المطروحة على المستشار

1. هل الإصلاح في §3 صحيح وكافٍ؟ وهل هناك مخالفات مشابهة لقاعدة `"use server"` قد تكون فاتت الفحص؟
2. **لماذا يستمر الخطأ بعد النشر؟** أي الفرضيات في §7 أرجح، وكيف تُثبت أو تُنفى؟
3. هل `type: "cache"` و`vercelCache: "BYPASS"` في السجلّ يشيران لسلوك كاش يستحق الفحص؟
4. هل تعدّد نشرات Preview على نفس المشروع قد يُسبّب تداخلاً؟
5. ما الخطوة التشخيصية التالية الأدقّ؟
6. هل هناك فحص وقائي (lint rule مثلاً) يمنع تكرار خطأ `"use server"` قبل النشر؟

---

## 10. مراجع سريعة

**الكومتات المعنيّة:**
```
5f40818  إصلاح "use server" — الحالي
9db7892  إلغاء التراجع — استعادة مراحل الأداء
f3716a2  التراجع (أُلغي) — كان خطأً
2cd9182  المرحلة 4 (أداء): N+1 الكتالوج + /api/health
8fa1e20  المرحلة 3 (أداء): getDashboardBundle
9bd5ed4  المرحلة 2 (أداء): connect_timeout + staleTime + شريط التنبيه
a66a5c9  المرحلة 1 (أداء): Promise.all في pnl.ts و getMonthlyProfit
c07d27d  ← جولة الفئات — هنا أُدخل الثابت المخالف
```

**ملفات محورية:**
- `src/features/finance/actions.ts` — موضع الخطأ
- `src/lib/db/client.ts` — إعداد الاتصال
- `src/middleware.ts` — المصادقة
- `src/providers/query-provider.tsx` — إعداد React Query
- `src/features/dashboard/components/DashboardClient.tsx` — الشاشة المتأثّرة
- `CLAUDE.md` (جذر المستودع) — قواعد المشروع وحالته

**ملف السجلّ:** `zman-app-api-server-log-export-2026-08-17T22-37-01.json`
